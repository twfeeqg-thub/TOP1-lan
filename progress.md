# خطة تنفيذ منصة "ذكاء سهل" (Thakaa Sahl)

## المهام المنجزة (Completed Tasks)
- [x] **المرحلة 0**: تأسيس جدول قاعدة البيانات المركزي `project_definitions` في سكيما `core` بالـ Supabase وحقن البيانات الأساسية بنجاح.
- [x] تثبيت مكتبات الاتصال وإدارة الحالة (`@supabase/supabase-js`, `@tanstack/react-query`).
- [x] تحديث ملف `metadata.json` بالاسم والوصف الصحيحين لمنصة "ذكاء سهل".
- [x] إنشاء كود الاتصال بـ Supabase من سكيما `core` في `/lib/supabase.ts` (باستخدام `db: { schema: 'core' }`).
- [x] إنشاء سياق الثيمات (Theme Provider) لدعم ثيمات Glassmorphism الثلاثة (فاتح، غامق، وردي) وحفظ الاختيار في الـ `localStorage` في `/app/providers.tsx`.
- [x] ضبط قالب التطبيق `/app/layout.tsx` ليدعم الاتجاه RTL افتراضياً واللغة العربية/الإنجليزية وتفعيل سياق الثيمات وموفر الاستعلامات (React Query Provider).
- [x] بناء المكونات الأساسية لصفحة الهبوط (`Header`, `Hero Section`, `Sectors Section`, `Footer`) وتجزئتها بطريقة احترافية مع دعم Suspense للتوافق مع Next.js 14/15.
- [x] تطوير منطق جلب المشاريع ديناميكياً وعرضها حسب حالة التفعيل (`is_active`).
- [x] اختبار الأداء والتوافق التام مع الجوال والشاشات المختلفة.
- [x] فحص وتأكيد نجاح التجميع (Build & Compile) بنسبة 100% وخلو الكود تماماً من أي مشاكل برمجية أو أخطاء TypeScript/ESLint.

## المهام الحالية (Current Tasks - المرحلة 1)
- [x] إطلاق وتأكيد الواجهة وجاهزيتها الكاملة للعرض والربط.

## المهام القادمة (Upcoming Tasks)
- [ ] العمل على المراحل القادمة (المرحلة 2) التي تشمل دمج الخدمات وإجراءات التحقق المتقدمة حسب طلبات الفريق.

