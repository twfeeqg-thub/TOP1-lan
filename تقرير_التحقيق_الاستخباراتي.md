# تقرير التحقيق الاستخباراتي - فحص اتصال قاعدة بيانات Supabase

**تاريخ التحقيق:** 25 يوليو 2026  
**الهدف:** تحديد أسباب عدم اتصال المنصة بقاعدة بيانات Supabase الفعلية واقتصارها على البيانات الوهمية (Mock Data)

---

## 1. فحص متغيرات البيئة (Environment Variables)

### الملفات الموجودة:
- `.env.local` ✅ — موجود في جذر المشروع.
- `.env.example` ✅ — موجود، يستخدم كمرجع.

### المتغيرات المطلوبة:
| المتغير | الحالة | القيمة |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ موجودة | `https://ydjbwnmjlqlthyfmxmqj.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ موجودة | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ موجودة | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` |
| `NEXT_PUBLIC_DEV_MODE` | ✅ موجودة | `true` |

**الخلاصة:** جميع متغيرات البيئة الضرورية موجودة وصالحة ظاهرياً. **لا يوجد عطل في متغيرات البيئة.**

---

## 2. فحص ملفات تهيئة الاتصال بقاعدة البيانات

### `lib/supabase.ts`
- ✅ يُنشئ `getSupabaseBrowserClient()` باستخدام `createClient` مع `schema: 'core'`.
- ✅ يُنشئ `getSupabaseAdminClient()` باستخدام `SUPABASE_SERVICE_ROLE_KEY` مع `schema: 'core'`.
- ⚠️ يحتوي على مصفوفة `fallbackProjects` — بيانات وهمية تُستخدم عند فشل الاتصال.
- ⚠️ **لا يُستخدم منفذ Connection Pooler (6543).** يستخدم URL Supabase الافتراضي (المنفذ 443) عبر REST API.

### `lib/supabase-pool.ts`
- ✅ يُنشئ `getPoolAdminClient()` باستخدام `createClient` مع `schema: 'core'`.
- ⚠️ **بالرغم من تسميته "pool"، إلا أنه لا يستخدم configure Connection Pooler بمنفذ 6543.** الاتصال يتم عبر REST API العادي.

### `lib/db.ts`
- 🗄️ هذا ملف لقاعدة بيانات **متصفح محلية (IndexedDB)** باستخدام `Dexie.js` — وليس له علاقة بـ Supabase. يُستخدم للتخزين المؤقت (Offline Cache).

**الخلاصة:** ملفات التهيئة صحيحة نحوياً، لكن **لا يوجد أي تكوين لـ Connection Pooler** بمنفذ 6543. الاتصال الحالي يذهب مباشرة إلى REST API الخاص بـ Supabase عبر المنفذ 443.

---

## 3. فحص منطق API و Fallback

### 3.1 مسارات Auth (المصادقة) — تحاول الاتصال بقاعدة البيانات فعلًا

| المسار | الملف | السلوك |
|---|---|---|
| `POST /api/auth/login` | `login/route.ts` | ✅ **يحاول الاتصال بـ Supabase عبر `poolAdmin`** — يستعلم جدول `users`، لا يوجد fallback. |
| `POST /api/auth/register` | `register/route.ts` | ✅ **يحاول الاتصال بـ Supabase عبر `poolAdmin`** — يُدخل في جدول `users` و `sessions`. |
| `GET /api/auth/check-phone` | `check-phone/route.ts` | ✅ **يحاول الاتصال بـ Supabase عبر `poolAdmin`** — يستعلم جدول `users`. |

**هذه المسارات فقط هي التي تحاول الاتصال بقاعدة البيانات فعلياً.**

### 3.2 مسارات Master (البيانات الرئيسية) — جميعها مُبرمَجة لتعيد البيانات الوهمية مباشرة دون محاولة اتصال

| المسار | الملف | السلوك |
|---|---|---|
| `GET /api/master/sectors` | `sectors/route.ts` | ❌ **يعيد `sectorsMock` مباشرة** — لا يوجد أي استعلام Supabase. |
| `POST /api/master/sectors` | `sectors/route.ts` | ❌ **يُضيف إلى `sectorsMock` في الذاكرة** — لا يُخزّن في قاعدة البيانات. |
| `GET /api/master/sectors/[sectorId]` | `sectors/[sectorId]/route.ts` | ❌ **يقرأ من `sectorFullData` (كائن في الذاكرة)** — لا استعلام Supabase. |
| `PUT /api/master/sectors/[sectorId]` | `sectors/[sectorId]/route.ts` | ❌ **يُحدّث `sectorFullData` في الذاكرة** فقط. |
| `DELETE /api/master/sectors/[sectorId]` | `sectors/[sectorId]/route.ts` | ❌ **يمسح من الذاكرة** فقط. |
| `GET /api/master/projects` | `projects/route.ts` | ❌ **يعيد `projectsMock` مباشرة** — لا استعلام Supabase. |
| `POST /api/master/projects` | `projects/route.ts` | ❌ **يُضيف إلى `projectsMock` في الذاكرة**. |
| `GET /api/master/features` | `features/route.ts` | ❌ **يعيد `featuresMock` مباشرة**. |
| `GET /api/master/ads` | `ads/route.ts` | ❌ **يعيد `adsMock` مباشرة**. |
| `GET /api/master/ads/requests` | `ads/requests/route.ts` | ❌ **يعيد `adRequestsMock` مباشرة**. |
| `GET/POST /api/master/ads/kill-switch` | `ads/kill-switch/route.ts` | ❌ **يستخدم `killSwitchMock` في الذاكرة**. |

### 3.3 `lib/get-sector-data.ts`
```typescript
export async function getSectorData(slug: string): Promise<SectorData | null> {
  const sector = sectorsMock.find(s => s.slug === slug && s.is_active)
  if (!sector) return null
  return sectorFullData[sector.id] ?? null
}
```
❌ **Hardcoded — يبحث فقط في البيانات الوهمية في الذاكرة. لا يوجد أي محاولة استعلام Supabase.**

### 3.4 `hooks/use-projects.ts` (جانب المتصفح)
```typescript
async function fetchProjectsWithCache() {
  // ...
  const { data, error } = await supabase.client
    .schema('core')
    .from('project_definitions')
    .select('*');
  if (error) throw error;
  // ...
}
```
✅ **هذا الهوك الوحيد الذي يحاول الاتصال بـ Supabase أولاً.**  
⚠️ لكنه يقع في الـ Fallback إلى `fallbackProjects` عند أي خطأ. إذا كانت جداول `project_definitions` أو `ads_engine` غير موجودة في قاعدة البيانات، فسيعيد البيانات الوهمية بصمت.

---

## 4. التشخيص النهائي — أين الخلل بالضبط؟

### الخلل الأساسي (جذري):
**معظم مسارات API (خاصة master data) مبرمَجة Hardcoded لتعيد البيانات الوهمية مباشرة دون حتى محاولة الاتصال بـ Supabase.** ليست هناك "آلية Fallback" تفشل — بل الأكواد لا تحاول أصلاً.

### الخلل الثانوي:
1. **جدول `project_definitions` و `ads_engine` في `schema 'core'` قد لا يكون موجوداً** في قاعدة بيانات Supabase. هوك `use-projects.ts` يحاول الاتصال، لكنه يفشل بصمت ويعود إلى `fallbackProjects`. لا يوجد ملفات `migrations` أو `schema.sql` في المشروع لإنشاء هذه الجداول.

2. **Connection Pooler (منفذ 6543) غير مُهيأ** في أي من ملفات الاتصال (`supabase.ts`, `supabase-pool.ts`). التسمية `poolAdmin` مضللة — فهي لا تستخدم الاتصال المُجمّع (Pooled Connection).

3. **نظام Auth (المصادقة) هو الجزء الوحيد الذي يحاول الاتصال بقاعدة البيانات فعلاً** عبر `lib/supabase-pool.ts`. إذا كانت جداول `users` و `sessions` غير موجودة، فستفشل المصادقة أيضاً.

### لماذا لا تتصل المنصة بقاعدة البيانات؟
- **مسار Master Data:** لأنه مكتوب ليعيد بيانات وهمية دون محاولة اتصال — هذا قرار برمجي (Hardcoded Logic) وليس عطلاً فنياً.
- **مسار Client Hooks:** لأنه يحاول الاتصال ولكن الجداول المطلوبة (`project_definitions`, `ads_engine`) إما غير موجودة في قاعدة البيانات أو أن `schema 'core'` غير مُنشأ — مما يؤدي إلى الخطأ والرجوع للبيانات الوهمية بصمت.

---

## 5. التوصيات

1. **إعادة كتابة مسارات Master API** لاستخدام `poolAdmin` (أو `supabaseAdmin`) للاستعلام من Supabase بدلاً من المصفوفات في الذاكرة.
2. **إنشاء ملفات Migrations** لإنشاء الجداول المطلوبة (`users`, `sessions`, `project_definitions`, `ads_engine`, إلخ) في `schema 'core'`.
3. **تصحيح Connection Pooler** — يجب استخدام رابط مثل:  
   `postgresql://[user]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true`  
   للاتصال المُجمّع عبر `@supabase/supabase-js` أو عبر `postgres.js` إذا أردنا الاتصال المباشر بـ PostgreSQL بدلاً من REST API.
4. **إزالة `fallbackProjects`** بعد التأكد من نجاح الاتصال بقاعدة البيانات، أو تحويله إلى لائحة افتراضية حقيقية تُستخدم فقط عند انقطاع الشبكة (Offline Mode).

---

*تم إعداد هذا التقرير بناءً على تحليل الكود المصدري دون إجراء أي تعديلات.*
