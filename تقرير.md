# تقرير الخطة الجمالية والنفسية — ثورة التجميل البصري والدعم النفسي

> تم إعداد الخطة: 26 يوليو 2026 — مرحلة التخطيط (Plan Mode)

---

## 1. تفعيل المهارات (Skills Activation)

| المهارة | الهدف |
|---------|-------|
| `frontend-design` | الوصول لمستوى استوديوهات التصميم العالمية — مدرسة **Modern Playful SaaS**: ألوان جريئة، زوايا مدورة ناعمة، طباعة معبرة، هوية بصرية لا تُنسى |
| `vercel-react-best-practices` | ضمان أداء حركات `motion/react` فائقة السلاسة عبر فصل `'use client'` في طبقات التفاعل فقط، تقليل إعادة التصيير، استخدام `will-change`، واحترام `prefers-reduced-motion` |

---

## 2. ثورة البطاقات والحركة — Premium Glassmorphism & Motion

### 2.1 بطاقات المشاريع (`DynamicProjects.tsx`)

| التعديل | الوصف |
|---------|-------|
| **التوسيع** | تغيير الشبكة من `md:grid-cols-2 lg:grid-cols-3` إلى `md:grid-cols-1 lg:grid-cols-2` مع `max-w-5xl` لبطاقات أعرض |
| **الزجاج** | تعزيز `glass-card` بحدود أدق (`0.5px` صلب/شفاف حسب الثيم)، `backdrop-blur-xl`، وظل داخلي ناعم |
| **Glow Hover** | إضافة عنصر وهمي `::after` بتوهج خلف البطاقة — لون متدرج (`var(--primary)` ← شفاف) يظهر فقط عند `hover` مع انتقال سبرنكي |
| **حركات الدخول** | تغليف كل بطاقة بـ `motion.div` مع `initial={{ opacity: 0, y: 40 }}` و `whileInView` بتأخير متدرج (`staggerChildren: 0.1`) |
| **Tooltips** | إضافة مكون `<SmartTooltip>` يظهر عند `hover` على الأزرار والأيقونات — محتوى داعم نفسياً (انظر البند 4) |

### 2.2 الأسئلة الشائعة (`DynamicFAQ.tsx`)
- استبدال `ChevronDown` الدوران بـ motion spring لمحتوى الإجابة: `AnimatePresence` + `motion.div` مع `layout` سبرنكي
- إضافة توهج خلف السؤال المفتوح

### 2.3 الشهادات (`DynamicTestimonials.tsx`)
- استبدال الـ `useState` index بـ `AnimatePresence` مع حركة `fade + slide` بين الشهادات
- إضافة مؤشر تقدم دائري متحرك بدلاً من النقاط الثابتة

### 2.4 الـ Hero (`DynamicHero.tsx`)
- إضافة motion للعناصر: ظهور متتابع (badge ← title ← description ← CTAs) مع spring
- إضافة Glow orb خلف الـ badge (عنصر زجاجي دائري بـ `filter: blur(60px)`)

---

## 3. تمرد الأيقونات الذكية — Memphis & Demographic

### 3.1 مكون `<IconFrame>` (جديد — `components/sector/IconFrame.tsx`)
- مكون يغلف أيقونات `lucide-react` داخل **مربع زجاجي** (96×96px) بخلفية متدرجة لونية
- خلفية التدرج: `background: linear-gradient(135deg, var(--primary-light), transparent)` + `backdrop-filter: blur(12px)`
- زوايا مدورة `rounded-2xl` + ظل خفيف

### 3.2 الفئة العمرية — أيقونات مزدوجة المسار

| الجمهور | النمط | الألوان | الميزات |
|---------|-------|---------|---------|
| **الطلاب (أطفال)** | أيقونات كرتونية مبهجة مع تأثيرات `bounce` | برتقالي/أصفر/وردي فاقع | تحريك خفيف `animate-bounce` أو `animate-float` عند hover |
| **المعلمون / الإداريون / أولياء الأمور** | أيقونات احترافية أنيقة مع توهج هادئ | أزرق/بنفسجي/ذهبي فاتح | تأثير `scale` طفيف + توهج |

**آلية التنفيذ**: إضافة حقل `audience: 'student' \| 'professional'` إلى نوع `Project` في `sector-types.ts`، يقرأه `<IconFrame>` لاختيار النمط المناسب.

### 3.3 تطبيق الأيقونات على المكونات السبعة

| # | المكون | الأيقونات المتأثرة | النمط |
|---|--------|-------------------|-------|
| 1 | `DynamicHero` | أيقونات الـ CTA (إن وجدت) | احترافي |
| 2 | `DynamicProjects` | أيقونة كل مشروع — تحويل من emoji إلى `lucide-react` مع `IconFrame` | حسب `audience` |
| 3 | `DynamicAbout` | `highlights[].icon` | احترافي |
| 4 | `DynamicTestimonials` | أيقونة `Quote` | احترافي |
| 5 | `DynamicFAQ` | `ChevronDown` | احترافي |
| 6 | `DynamicPartners` | (شعارات الشركاء — لا حاجة لأيقونات) | - |
| 7 | `DynamicLegalFooter` | (نصوص قانونية — لا حاجة لأيقونات) | - |

---

## 4. الشرط السيادي المطلق — عبارات الدعم النفسي

### 4.1 مصفوفة العبارات المركزية — ملف جديد `lib/psych-support.ts`
- `welcomeMessages`: للـ Hero
- `loadingMessages`: للتحميل
- `encouragementMessages`: لبطاقات المشاريع
- `hoverMessages`: للأزرار
- `emptyMessages`: للفراغات

### 4.2 التوزيع على المكونات السبعة

| # | المكون | نوع الرسالة | الموقع | مثال |
|---|--------|------------|--------|------|
| 1 | **Hero** | رسالة ترحيبية | أسفل الـ title أو كـ subtitle عائم | *"نحن هنا لنجعل رحلتك التعليمية أسهل وأكثر متعة"* |
| 2 | **Projects** | رسالة تشجيعية | داخل كل بطاقة (شريط علوي) + عند hover الأزرار | *"خطوة واحدة تفصلك عن التميز"* |
| 3 | **About** | رسالة طمأنة | قبل `highlights` | *"كل أداة صممت بخبراء التعليم لتناسب احتياجاتك"* |
| 4 | **Testimonials** | رسالة تقدير | أسفل اسم العميل | *"شريك نجاحك هو مصدر فخرنا"* |
| 5 | **FAQ** | رسالة طمأنة + تشجيع | عند فتح سؤال | *"سؤال رائع! دعنا نوضح لك الأمر"* |
| 6 | **Partners** | رسالة ثقة | أعلى شبكة الشركاء | *"نفتخر بثقة شركائنا الرائدين"* |
| 7 | **LegalFooter** | رسالة وداع لطيفة | أسفل التذييل | *"نتمنى لك يوماً دراسياً مليئاً بالإنجاز"* |

### 4.3 آلية التنفيذ
- إنشاء motion spring لكل رسالة عند ظهورها (تأثير fade-in مع slide-up)
- تدوير الرسائل عشوائياً من المصفوفة عند إعادة التحميل
- دعم RTL كامل (النصوص بالعربية)
- احترام `prefers-reduced-motion` (إظهار النص بدون حركة)

---

## 5. هيكل الملفات الجديدة والمعدّلة

### ملفات جديدة:

| المسار | الوصف |
|--------|-------|
| `lib/psych-support.ts` | مصفوفة عبارات الدعم النفسي + دالة الاختيار العشوائي |
| `components/sector/IconFrame.tsx` | مكون تغليف الأيقونات (زجاجي متدرج + دعم الفئة العمرية) |
| `components/sector/SmartTooltip.tsx` | مكون التلميحات الذكية مع رسائل دعم نفسي |

### ملفات معدّلة:

| المسار | التعديل |
|--------|---------|
| `components/sector/DynamicHero.tsx` | إضافة motion spring + رسالة ترحيبية + Glow orb |
| `components/sector/DynamicProjects.tsx` | بطاقات أعرض + Glow hover + IconFrame + SmartTooltip + رسالة تشجيعية |
| `components/sector/DynamicAbout.tsx` | IconFrame للـ highlights + رسالة طمأنة |
| `components/sector/DynamicTestimonials.tsx` | AnimatePresence + رسالة تقدير |
| `components/sector/DynamicFAQ.tsx` | Motion spring للإجابات + رسالة طمأنة عند الفتح |
| `components/sector/DynamicPartners.tsx` | رسالة ثقة أعلى الشبكة |
| `components/sector/DynamicLegalFooter.tsx` | رسالة وداع أسفل التذييل |
| `components/sector/index.ts` | إضافة تصدير `IconFrame` و `SmartTooltip` |
| `lib/sector-types.ts` | إضافة حقل `audience` إلى `Project` |
| `app/globals.css` | إضافة متغيرات توهج جديدة + كلاسات `glow-card` و `icon-frame` |

### ملفات بدون تعديل:

| الملف | السبب |
|-------|--------|
| `app/page.tsx` | الصفحة الرئيسية — خارج نطاق القطاع التعليمي |
| `lib/get-sector-data.ts` | لا تغيير في منطق جلب البيانات |
| `lib/sectors-mock-data.ts` | تعديل فقط عند الحاجة لإضافة `audience` |
| جميع ملفات `auth/` | خارج نطاق القالب التعليمي |
| جميع ملفات `ads/` | خارج نطاق القالب التعليمي |
| جميع ملفات `ui/` | خارج نطاق القالب التعليمي |
| جميع ملفات `audit/` | خارج نطاق القالب التعليمي |

---

## 6. الاستبعادات

- ⛔ **chrome-devtools MCP**: يُمنع استخدامه أو فحصه تماماً في أي مرحلة من مراحل التنفيذ
- ⛔ لا يُسمح بتعديل الثيمات الثلاثة (فاتح، غامق، وردي)
- ⛔ لا يُسمح بالمساس بالهيكل البرمجي الأساسي (package.json, next.config, tsconfig, eslint)
- ⛔ لا يُسمح بتغيير `DynamicPartners` جوهرياً (فقط إضافة رسالة ثقة)

---

## 7. خطة التنفيذ المقترحة (بالترتيب)

| المرحلة | المدة | المهام |
|---------|-------|--------|
| **1: التأسيس** | جلسة واحدة | إنشاء `lib/psych-support.ts` + `IconFrame.tsx` + `SmartTooltip.tsx` |
| **2: العبارات** | جلسة واحدة | حقن عبارات الدعم في المكونات السبعة |
| **3: الحركة** | جلسة واحدة | Motion spring لـ FAQ + Testimonials + Hero |
| **4: البطاقات** | جلستان | إعادة تصميم DynamicProjects + Glow + التوسيع |
| **5: الأيقونات** | جلسة واحدة | تطبيق IconFrame على المشاريع + الـ About |
| **6: المراجعة** | جلسة واحدة | `npm run build` + اختبار يدوي لكل ثيم + RTL + Reduced Motion |

---

> **ملاحظة**: هذه الخطة قابلة للمراجعة والاعتماد. بعد الموافقة، تبدأ مرحلة التنفيذ (Build Mode).
