# 📋 تقرير الفحص الاستخباراتي — جودة وأمان قاعدة البيانات (QA & Security)

> **الحالة:** ✅ الاتصال سليم — الجداول مكتملة ومطابقة — الـ 403 زائل تماماً
> **تاريخ التقرير:** 5 أغسطس 2026
> **البيئة المستهدفة:** مشروع `ydjbwnmjlqlthyfmxmqj` — PostgreSQL Pooler على المنفذ `6543`
> **نطاق العمل:** تدقيق `.env.local` + اختبار اتصال + مطابقة الجداول الحية + معالجة النقص + فحص الصلاحيات (403) عبر anon/service_role

---

## 1) حالة الاتصال المحلي (`.env.local`) — سليم ✅

| البند | النتيجة |
|---|---|
| وجود `DATABASE_URL` | ✅ موجود — `postgresql://postgres.***@aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true` |
| الاتصال بالـ Pooler `:6543` | ✅ **OK** عبر مكتبة `pg@8.22.0` مع `ssl` |
| اختبار الاستعلام الأساسي `SELECT 1` | ✅ `{"test": 1}` |
| قاعدة البيانات / المستخدم | `postgres` / `postgres` |
| المفاتيح المحلية | `anon` + `service_role` مفكّكتان بنجاح وتنتميان لنفس المشروع |

---

## 2) الحالة الحقيقية قبل المعالجة — كشف خاطئ لم يُطبَّق

> **نقطة حرجة:** سكربت `scripts/migration_live_connect.sql` **لم يُطبَّق فعلياً على هذه القاعدة** رغم الادعاء بتشغيله عبر DBeaver. الأدلة:

| المؤشر | الواقع قبل المعالجة |
|---|---|
| جداول مفقودة كلياً في أي سكيما | `core.sectors` — `core.features` — `core.schools` — `core.ad_requests` — `core.kill_switch` (404 / PGRST205) |
| `core.ads_engine` بأعمدة قديمة | `id, campaign_name, ad_config, is_active, created_at` فقط — بلا `media_url/status/clicks/impressions/budget/platform/request_id` |
| `core.master_audit_log` بنية مكسورة | بلا `severity`؛ `entity_id` من نوع `uuid` و`details` من نوع `jsonb` (سيكسر `logAudit()` مع المعرّفات النصية والنصوص العربية) |
| أخطاء الـ 403 حقيقية ومستمرة | `users/sessions/master_audit_log/global_features` رفضت القراءة حتى بمفتاح `service_role` (و401 عبر anon) |
| المنح جزئية | صلاحيات كاملة لـ 3 جداول فقط (`ads_engine`, `project_definitions`, `schema_template`) |
| جداول خارج السكيما | **لا توجد** أي جداول مكررة خارج `core` — سكيما `public` فارغة، وسكيمات `edu_*` تملك جداولها الشرعية |

---

## 3) المعالجة المنفَّذة — `scripts/remediation_qa_fix.sql` (جديد)

تمت كتابة السكربت وتشغيله مباشرة عبر الـ Pooler بنجاح (وآمن لإعادة التشغيل / Idempotent):

| الإجراء | التفصيل |
|---|---|
| 1) إنشاء `core.sectors` | `id text PK, name, slug UNIQUE, icon, is_active, full_data jsonb, created_at` |
| 2) بذر القطاعات | `edu-1/التعليم/education` نشط ✅ + `health` + `realestate` + `commerce` (معطّل) — `ON CONFLICT` إعادة إدراج آمنة |
| 3) إنشاء `core.features` + `core.schools` | بنية مطابقة لتوقعات مسار الميزات |
| 4) إنشاء `core.ad_requests` | `client_info/campaign/attachments/design_request` كـ JSONB + `status` |
| 5) إنشاء `core.kill_switch` | صف وحيد `id=true` |
| 6) توسعة `core.ads_engine` | إضافة `media_url, status, clicks, impressions, budget, platform, request_id` + إعادة حساب `status` من `is_active` |
| 7) إصلاح `core.master_audit_log` | إضافة `severity`؛ تحويل `entity_id → text` و`details → text`؛ إتاحة `user_id/entity_type/entity_id` بلا NOT NULL |
| 8) المنح الشاملة | `USAGE` + `ALL` على كل الجداول/التسلسلات في `core` للأدوار الثلاثة + `ALTER DEFAULT PRIVILEGES` |
| 9) تحديث كاش PostgREST | `NOTIFY pgrst, 'reload schema'` |

**التنظيف:** لا حاجة لمسح أي شيء — لا توجد جداول منشأة خارج السكيما المحددة.

---

## 4) نتائج التحقق بعد المعالجة — جاهز للتشغيل ✅

| التحقق | النتيجة |
|---|---|
| قراءة قطاع التعليم | ✅ `{id:"edu-1", name:"التعليم", slug:"education", is_active:true, full_data:{}}` |
| الجداول العشرة المطلوبة | ✅ موجودة وبهيكل مطابق للكود: `sectors/users/sessions/master_audit_log/global_features/ads_engine/features/schools/ad_requests/kill_switch` |
| REST عبر `anon` (سكيما `core`) | ✅ **200** لجميع الجداول التسعة (كانت 403/404) |
| REST عبر `service_role` | ✅ **200** لجميع الجداول التسعة |
| اختبار كتابة `logAudit()` | ✅ إدراج ناجح بمعرّف نصي غير-uuid + `user_id` فارغ + نص عربي، ثم حذف |
| إدراج إعلان بالأعمدة الجديدة | ✅ ناجح مع `status/clicks/impressions` |
| تغطية المنح | ✅ 14 جدول × 7 صلاحيات × الأدوار الثلاثة |
| إعادة تشغيل السكربت | ✅ بلا أخطاء (Idempotent) |
| مسار العرض العام | ✅ `get-sector-data` و`use-projects` يستخدمان `.schema('core')` صراحةً — مطابق لما تم التحقق منه |

---

## 5) ملاحظات أمنية (توصية لا عائق)

- منح `anon` صلاحيات كاملة على `users`/`sessions` يتعارض مع أفضل الممارسات. **يوصى قبل الإطلاق العام:** تفعيل **RLS** (Row Level Security)، وتقييد `anon` بالقراءة العامة فقط، وإبقاء الكتابة الحساسة عبر `service_role`.
- `JWT_SECRET` المحلي قيمة مؤقتة (`aisahl-super-secret...`) — استبدالها إلزامي قبل الإنتاج.
- `DATABASE_URL` يحتوي كلمة مرور حقيقية داخل `.env.local` المتعقَّب في Git — يُنصح بنقل الأسرار إلى ملف محلي خارج الإصدار.

---

## 6) ملفات العمل

- **جديد:** `scripts/remediation_qa_fix.sql` (معالجة إصلاحية idempotent).
- **أدوات فحص (خارج المستودع في /tmp):** سكربتات اتصال وتدقيق وفحص REST.

---

## 7) الخلاصة

المنصة **قابلة للتشغيل الحي** على الـ Pooler `6543`:
- الاتصال سليم والاستعلام الأساسي ناجح.
- الجداول كاملة ومطابقة لتوقعات الكود سطراً بسطر.
- أخطاء الحظر 403 زالت تماماً لكل من `anon` و`service_role`.

> ⚠️ **تنبيه للتحقيق:** اتصال DBeaver السابق شُغِّل على قاعدة/مشروع آخر — السكربت القديم لم ينعكس على `ydjbwnmjlqlthyfmxmqj`. تعتمد المعالجة الآن على `remediation_qa_fix.sql`.

---

## 8) تقرير الحقن والتشخيص النهائي — خطأ `hero.badge` (5 أغسطس 2026)

### 8.1) الجذر الفعلي للخطأ (Root Cause)

- الخطأ `Cannot read properties of undefined (reading 'badge')` عند `components/sector/DynamicHero.tsx:31`.
- السبب: `core.sectors` موجودة في السحابة وبها صف القطاع التعليمي (`edu-1/education`)، لكن عمود `full_data` كان **`{}` فارغاً** لجميع القطاعات الأربعة.
- `getSectorData` يعتبر `{}` قيمة صحيحة (Truthy) فيرجع `data = {}`، فيُمرَّر `undefined` إلى `hero.badge` → انهيار الصفحة.
- **الجدول لم يكن مفقوداً ولا كانت المنح 403 — المشكلة كانت غياب الحقن الكامل للبيانات** (`hero/projects/about/...`).

### 8.2) التحقق المباشر من السحابة (نفّذ عبر psql على Pooler :6543)

| البند | النتيجة |
|---|---|
| الاتصال بـ `DATABASE_URL` على المنفذ `6543` | ✅ `postgres@postgres — PostgreSQL 17.6` |
| جداول `core` المنشأة بكامل أعمدتها | ✅ **14 جدولاً**: `sectors, users, sessions, master_audit_log, ads_engine, features, schools, ad_requests, kill_switch, global_features, master_users, master_permissions, project_definitions, schema_template` |
| أعمدة `ads_engine` الموسّعة (`media_url/status/clicks/impressions/budget/platform/request_id`) | ✅ موجودة |
| سكيبا `master_audit_log` (`severity/entity_id text/details text`) | ✅ مطابقة لعقد `logAudit()` |
| منح الأدوار الثلاثة على `core` | ✅ `anon/authenticated/service_role` — كل الجداول بكل الصلاحيات + `USAGE` على السكيما |
| سياسات RLS على `core.sectors` | ✅ غير مفعّلة (قراءة عامة سليمة) |
| صف `kill_switch` الوحيد | ✅ موجود |
| **محتوى `full_data`** | ❌ **`{}` فارغ لجميع القطاعات** → جذر الخطأ |

### 8.3) السكربتات المنفَّذة بنجاح

| السكربت | النتيجة |
|---|---|
| `scripts/migration_live_connect.sql` | ✅ نُفّذ عبر الـ Pooler — Idempotent، كل الجداول والأعمدة والمنح قائمة، `NOTIFY pgrst` أُرسل |
| `scripts/remediation_qa_fix.sql` | ✅ نُفّذ عبر الـ Pooler — المحاذاة والمنح مؤكَّدة، `NOTIFY pgrst` أُرسل |
| **حقن `full_data`** (4 تحديثات مولّدة من `lib/sectors-mock-data.ts`) | ✅ `UPDATE 1 × 4` — `edu-1/health-1/realestate-1/commerce-1` |
| `NOTIFY pgrst, 'reload schema'` | ✅ |

### 8.4) تأكيد البيانات الحية (SQL + REST)

| التحقق | النتيجة |
|---|---|
| SQL | ✅ `edu-1 → hero.title="سهل - ذكاء رقمي تعليمي"`، `5` مشاريع، `3` أسئلة شائعة |
| REST عبر `anon` (سكيما `core`, `slug=eq.education`) | ✅ **200** → `{"id":"edu-1","slug":"education","is_active":true,"title":"سهل - ذكاء رقمي تعليمي"}` |
| تجوال `npm run dev` على `/education`, `/health`, `/realestate` | ✅ **HTTP 200** للثلاثة — لا أثر لخطأ `badge` |

### 8.5) حصانة إضافية للكود (QA)

- `lib/get-sector-data.ts`: أُضيف فحص دفاعي — إذا كان `full_data` فارغاً أو بلا `hero` يُرجع `null` (→ `notFound()`) بدل تمرير بيانات ناقصة تكسر الصفحة.
- `eslint lib/get-sector-data.ts` ✅ بلا أخطاء.

### 8.6) الخلاصة النهائية

- القاعدة السحابية **مكتملة البنية والمنح** بعد تشغيل السكربتين (كلاهما Idempotent).
- الخلل الوحيد الذي كان يكسر صفحة القطاع **عولج** بحقن `full_data` الكامل في السحابة الحقيقية + حاجز دفاعي في طبقة القراءة.
- المسارات الثلاثة النشطة تعمل الآن من البيانات الحية مباشرة (بدون Fallback).
