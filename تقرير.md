# 📋 تقرير التنفيذ الفعلي — المهام 1-8 (نظام التحكم ذو الأربع طبقات)

> **الحالة:** ✅ **مكتمل — تم تنفيذ جميع المهام والتحقق منها**
> **التاريخ:** 08-08-2026
> **التحقق النهائي:** `tsc --noEmit` ✅ · `eslint .` ✅ (0 أخطاء) · `npm run build` ✅
> **آخر التزام (commit):** `23742a34` — قبل بدء هذه الجلسة (الأعمال الجديدة غير ملتزمة بعد)

---

## 🧹 ملخص الجلسة

نُفّذت **المهام 1 إلى 8** من المخطط المعماري بالكامل مع معالجة جميع الاكتشافات الحاسمة (D1–D8). لم تُنشأ أي أعمدة قاعدة بيانات جديدة — كل الإضافات عبر مفاتيح JSONB ودلو تخزين واحد، مع سكربتي هجرة محروسين بمنفذ 6543 (وضع التجمّع).

---

## ✅ المهمة 1 — إصلاح عدم تطابق الـ Hydration واستعادة التنقل

### 1أ) `app/master/versions/v2/Sidebar.tsx`
- استُخدم `useHasHydrated()` لحماية كتلة `super_admin` (سطر 52) وكتلة الصورة الرمزية/التذييل.
- كتلة `SUPER_ADMIN_JUMPS` تُعرض فقط داخل حماية الـ hydration.

### 1ب) `app/master/components/nav-items.tsx`
- أُعيدت إضافة عنصري **«المستخدمون»** (`roles: ['super_admin']`) و**«الإعدادات»** (`roles: ['super_admin', 'master']`).
- `getNavItems('super_admin')` يُرجع الكل؛ `master` يحصل على الإعدادات فقط.

### 1ج) صفحات جديدة (جُلبت من الوضع "logic README فقط" إلى صفحات فعلية)
- **`app/master/users/page.tsx`** — جدول قراءة فقط لـ `core.users` مقيّد بدور `super_admin`، مع رسائل نفسية (تحميل/فارغ) وزر تحديث.
- **`app/master/settings/page.tsx`** — غلاف إعدادات مقيّد بـ `['super_admin', 'master']` يستضيف `<BrandingUploader />` (المهمة 4) + بطاقة معلومات إصدار الكاش.

### 1د) `app/master/components/require-role.tsx`
- مكوّن عميل `RequireRole({ role })` يعرض الأطفال للأدوار المسموحة وإلا بطاقة «ليس لديك صلاحية».

---

## ✅ المهمة 2 — رصّ شبكة القطاعات وربط مفاتيح التبديل

### 2أ) `components/layout-engine/DynamicSectorsGrid.tsx` + `app/master/sectors/page.tsx`
- توحيد الشبكة إلى `grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 w-full` في الموقعين.

### 2ب) `components/layout-engine/SectorControlCard.tsx`
- أُزيل تراكب `GripVertical` المطلق؛ أُبقيت حركة الإحداثيات فقط (`opacity/y/delay`) دون override على التموضع.

### 2ج) `app/master/components/sectors/SectorCard.tsx`
- أصبح الكارت مكتفياً ذاتياً: `useMutation` داخلي يستدعي `PUT /api/master/sectors/{id}` بـ `{ is_active }`، مع إبطال استعلامات `['master-sectors']` + `['master-audit']` و UI تفاؤلي. `onToggle` بقي كاحتياط اختياري.

### 2د) القطاعات غير النشطة على الرئيسية
- **`hooks/use-sectors.ts`** جديد — استعلام React Query يجلب `core.sectors` (عميل anon) بـ `select('id, name, slug, icon, is_active')`.
- **`app/page.tsx`** — دمج بيانات DB فوق `SECTOR_META`؛ غير النشط يُعرض بتعتيم `opacity-60` + شارة «قيد التطوير» + النقر يفتح مودال الاقتراحات.
- **`lib/get-sector-data.ts`** — أُضيف `.eq('is_active', true)` فلا يُخدم مسار DB القطاعات غير النشطة.

---

## ✅ المهمة 3 — استرجاع الجمل/الهياكل متعددة اللغات (i18n)

### 3ب) `lib/i18n-sector.ts` (جديد)
- `resolveLangText(root, en, lang)` → `lang === 'en' ? (en ?? root) : (root ?? '')` (تراجع تلقائي إلى العربية).
- `resolveSectorForLang(data, lang)` يحلّ أقسام hero/projects/about/testimonials/faqs/partners/legal_footer ثنائية اللغة دفعة واحدة، + تصدير نوع `SectorLang`.

### 3ب) `lib/sector-types.ts`
- توسيع الأنواع بحقول اختيارية `*_en`: `Hero (badge_en/title_en/description_en)`, `Project (name_en/description_en/features_en)`, `About`, `Testimonial (role_en/content_en)`, `FAQ (question_en/answer_en)`, `Partner (name_en)`, `LegalFooter (compliance_text_en/meta_rights_text_en)`, `CTA (text_en)`.

### 3ج) `lib/get-sector-data.ts`
- إعادة تصدير `resolveSectorForLang` + `SectorLang`؛ المتصلون يمررون اللغة النشطة.

### 3د) عارضات الصفحات الفرعية (7 مكوّنات)
- جميعها تقرأ `const { lang } = useApp()` وتحل الحقول عبر `resolveLangText`:
  `DynamicHero`, `DynamicProjects`, `DynamicAbout`, `DynamicTestimonials`, `DynamicFAQ`, `DynamicPartners`, `DynamicLegalFooter` (بما فيها ترجمة عناوين روابط السياسات).

### 3هـ) سكربتا الهجرة
- **`scripts/migration_sector_i18n.sql`** — `UPDATE core.sectors SET full_data = jsonb_set(...)` يملأ `hero.title_en/description_en/badge_en`، `about.*_en`، `projects[].*_en`، `testimonials[].*_en`، `faqs[].*_en`، `legal_footer.*_en` للقطاعات الأربع (`edu-1`, `health-1`, `realestate-1`, `commerce-1`). صيغة `COALESCE` تحافظ على أي قيمة `_en` موجودة سابقاً، مع حماية `full_data ? 'hero'` + كتلة تحقق `DO`.
- **`scripts/migrate-sector-i18n.ts`** — منفّذ يتحقق من منفذ 6543، معاملة، `NOTIFY pgrst`، جدول تحقق بصري.
- **package.json** — سكربت `"db:migrate:i18n"`.

---

## ✅ المهمة 4 — مرفّع الصور الأساسي للمنصة (خيار البراندنغ)

### 4أ) `components/admin/BrandingUploader.tsx` (جديد)
- ثلاث فتحات أصول: **الشعار** `512×512` WebP، **أيقونة المتصفح** `32×32` ICO، **أيقونة PWA** `512×512` PNG.
- مدخلات: سحب-وإفلات + منتقي ملفات + حقل رابط (وضع الرابط يتحقق من الامتداد ويخزن الرابط مباشرة).
- تحقق عميل: MIME/امتداد، حد `1.5MB`، فحص الأبعاد عبر `readDimensions`.
- خصائص كلها اختيارية (`tenantId='platform'`, `value`, `onSaved`, `onBeforeSave`) — متوافق مع الاستخدام في `/master/settings` (بلا خصائص).

### 4ب) مسارا الـ API
- **`app/api/admin/branding/upload/route.ts`** — runtime nodejs، مصادقة خادم عبر `resolveSessionFromRequest`، بوابة `canUploadBranding`، رفع عبر `poolAdmin.client.storage.from('platform-assets').upload('branding/{tenantId}/{assetType}.{ext}', ..., { upsert: true, contentType })`، فحص MIME + 1.5MB، ويعيد `{ data: { url, path } }`.
- **`app/api/admin/branding/url/route.ts`** — مدقق-فقط (بلا جدول DB)؛ الرابط يُحفظ عميلياً عبر `onSaved`.

### 4ج) علم الميزة + بوابة الدور
- **`lib/branding-gate.ts`** — `canUploadBranding({ role, customUploadFlag })`: `super_admin`/`master` مسموح دائماً؛ البقية فقط إذا `customUploadFlag === true`.
- **`lib/workspace-types.ts`** — `EditableWorkspaceConfig` اكتسب `logo_url?`, `favicon_url?`, `pwa_icon_url?` + `feature.branding.custom_upload?`.
- **`components/client/BrandingTab.tsx`** — قراءة العلم من `value.feature.branding.custom_upload` عبر `useAuth`، يعرض المرفّعات خلف البوابة ويربط `ASSET_KEY` لكل نوع أصل.
- **`app/client/[tenantId]/[projectSlug]/page.tsx`** — يمرر `tenantId={tenantId}` إلى `BrandingTab`.

### 4د) إعداد التخزين
- **`scripts/setup_branding_bucket.sql`** — دلو `platform-assets` عام (ON CONFLICT DO NOTHING) + 3 سياسات: قراءة عامة، كتابة service-role، حذف مقيّد بالمالك.
- **`scripts/migration_branding_feature.sql`** — ضبط `modules_config.feature.branding.custom_upload = true` لمشاريع حقيقية مزروعة: `edu_schools`, `edu_exam`, `edu_twin`, `health_clinic`.
- **`scripts/migrate-branding-feature.ts`** — منفّذ بنمط 6543 + `NOTIFY pgrst`.
- **package.json** — سكربت `"db:migrate:branding"`.

---

## ✅ المهمة 5 — معالجة فشل تعديل القطاع الفارغ/الجديد

### 5أ) `lib/sector-types.ts`
- أُضيف `normalizeSectorData(record: Partial<SectorData> | null | undefined): SectorData` باستخدام `deepMerge` من `lib/overrides-merge.ts` (صفر/null يُبقي الأساس، المصفوفات تُستبدل).

### 5ب) `app/master/sectors/[sectorId]/page.tsx`
- `setLocalData(normalizeSectorData(data.data))` بدل النسخ الخام.

### 5ج) `SectorFormWrapper.tsx` + نماذج الأقسام السبعة
- حزام دفاعي: `const safe = normalizeSectorData(data)` وتمرير `safe.hero/projects/...` للنماذج.
- الحرس: `HeroSectionForm` (`cta_primary/cta_secondary ?? { text:'', href:'' }`)، `AboutSectionForm` و`LegalFooterSectionForm` (`?? []` و`data?.title ?? ''`)، ونماذج المصفوفات الأربعة (Projects/Testimonials/FAQ/Partners) تستخدم `const safeData = data ?? []` (متغير محلي بدل إعادة تعيين prop لتفادي خطأ `react-hooks/immutability` في eslint).

---

## ✅ المهمة 6 — أزرار CTA للإعلانات، التتبع، وتصدير التقارير

### 6أ) `components/ads/ad-cta-button.tsx` (جديد)
- `AdCtaButton` يقبل `{ lang, onOpenRequest }`، يفتح `<ClientAdRequestModal>`، نصوص عربية/إنجليزية.
- مضمّن داخل `Ad_Renderer_Component` بعد كارت الإعلان. قُصر عمداً مع كتلة الإعلان للمميزين/kill-switch (القرار رقم 3 — موثق في الخطة).

### 6ب) `app/api/ads/track/route.ts` (جديد)
- zod schema: `{ ad_id: string.min(1), action: 'impression'|'click', slot?, lang? }`.
- تحديث ذري ببيان SQL واحد `CASE WHEN` على `clicks`/`impressions` دون سباق قراءة-ثم-كتابة.
- مسار عام (خارج matcher الـ middleware) — يُرجع `{ ok: true }` دائماً ولا يُطلق 500 أبداً (catch → 200 محايد).

### 6ج) التتبع في `components/ad-renderer.tsx`
- **المشاهدات:** `useEffect` بمفتاح `ad.id` + حارس `useRef` ضد ازدواج StrictMode، مع `keepalive: true`، يُطلق فقط عند `killSwitchLoaded && !killSwitchActive && !isPremiumUser`.
- **النقرات:** في معالج «View Offer» → `fetch('/api/ads/track', { ad_id, action:'click' })` قبل فتح الرابط.
- `AdCtaButton` معروض داخل حاوية الفتحة.
- **`app/page.tsx`:** أُزيلت حالة `adRequestModal` + الزر المستقل «أضغط هنا لإضافة إعلانك الخاص» + استيراد `ClientAdRequestModal` (انتقل للمكوّن).

### 6د) تصدير التقرير
- **`lib/ads-report.ts`** (جديد): `buildAdsCsv`, `buildAdsTxt` (مع UTF-8 BOM لدعم العربية في Excel/WhatsApp), `downloadBlob`, `waShareUrl`, `emailShareUrl`.
- **`app/master/ads/page.tsx`:** زر «تصدير التقرير» بقائمة CSV/TXT + مشاركة سريعة WhatsApp/Email (استُخدم `useMemo` للأدوات لتفادي تحذير eslint).

---

## ✅ المهمة 7 — خروج نظيف وتطهير كامل لكاش PWA

### 7أ) `context/AuthContext.tsx`
- `logout` يضيف: تطهير كاش PWA (`caches.keys()` → حذف الكل) + `window.location.replace('/')` انتقال قسري (مع الاحتفاظ بمسح `db.projects`/`db.outbox`).

### 7ب) `public/sw.js`
- `CACHE_NAME` رُفع إلى `aisahl-static-v2`.

### 7ج) `components/service-worker-register.tsx`
- مستمع `controllerchange` → إعادة تحميل **مرة واحدة** فقط (حارس `hadController`) ليتولى الـ SW الجديد دون إعادة تحميل يدوية أو حلقات لا نهائية.

---

## ✅ المهمة 8 — حقن الرسائل النفسية

### 8أ) `lib/psych-support.ts`
- أُضيف `loginErrorMessages` + `logoutFarewellMessages` بجانب المصفوفات الموجودة.

### 8ب) `app/login/page.tsx`
- توست الخطأ يعرض رسالة نفسية دافئة (`usePsychMessage(loginErrorMessages)`) بخط عريض + الخطأ التقني تحته.
- مسار النجاح: توست ترحيب غير محجوب `usePsychMessage(welcomeMessages)` يختفي بعد 2 ثانية قبل التوجيه.

### 8ج) وداع الخروج
- **`components/auth/LogoutFarewell.tsx`** (جديد) — طبقة ملء الشاشة (نص وداع + عداد/حركة)، توجيه ناعم بعد **1200ms** واحتياط صارم `window.location.replace('/')` بعد **1500ms** (لا يقفل المستخدم أبداً).
- `AuthContext` يضيف حالة `logoutFarewell` ويضبطها بـ `getRandomMessage(farewellMessages)` في `logout` (أُزيل الـ replace الفوري — الطبقة تتولى التنقل)، ويُعرض `<LogoutFarewell message={logoutFarewell} />` داخل `AuthProvider`.

### 8د) مودالات القطاعات
- **`app/master/sectors/page.tsx`:** نجاح مودال الإنشاء يعرض توست `getRandomMessage(successMessages)` (يختفي بعد 3 ثوانٍ). (لا يوجد تدفق حذف حالياً في الصفحة — لم يكن جزءاً من الحالة الفعلية).

---

## 🧪 التحقق النهائي
| الفحص | النتيجة |
|---|---|
| `npx tsc --noEmit` | ✅ 0 أخطاء |
| `npx eslint .` | ✅ 0 أخطاء (أُصلحت تحذيرات immutability + useMemo) |
| `npm run build` | ✅ نجح (جميع الصفحات بُنيت) |

### أخطاء اكتُشفت وصُححت أثناء التحقق
1. `app/login/page.tsx` — الاستخدام الخاطئ لـ `usePsychMessage` (لا يأخذ theme، بل مصفوفة رسائل) → `psychError`/`psychWelcome` منفصلان.
2. `lib/ads-report.ts` — أعمدة رقمية تحتاج `String(...)` في الـ CSV.
3. `lib/i18n-sector.ts` — إرجاع `About | undefined` → حارس `?? (data.about as About)`.
4. ملفات نماذج القطاعات — إعادة تعيين prop (`data = data ?? []`) تخالف `react-hooks/immutability` → متغير محلي `safeData`.

---

## 📦 الملفات الجديدة (غير ملتزمة بعد)
- `app/api/admin/branding/upload/route.ts`, `app/api/admin/branding/url/route.ts`
- `app/api/ads/track/route.ts`
- `app/api/master/users/route.ts`
- `app/master/users/page.tsx`, `app/master/settings/page.tsx`
- `app/master/components/require-role.tsx`
- `components/admin/BrandingUploader.tsx`
- `components/ads/ad-cta-button.tsx`
- `components/auth/LogoutFarewell.tsx`
- `hooks/use-sectors.ts`
- `lib/i18n-sector.ts`, `lib/ads-report.ts`, `lib/branding-gate.ts`
- `scripts/migrate-sector-i18n.ts`, `scripts/migrate-branding-feature.ts`
- `scripts/migration_sector_i18n.sql`, `scripts/migration_branding_feature.sql`, `scripts/setup_branding_bucket.sql`

## 📝 ملاحظات للتنفيذ الفعلي (ما زال يلزمك تنفيذه)
1. **تشغيل الهجرات** على قاعدة البيانات الفعلية:
   - `npm run db:migrate:i18n`
   - `npm run db:migrate:branding`
   - تنفيذ `scripts/setup_branding_bucket.sql` (دلو التخزين + السياسات).
2. **الالتزام (commit)** — الأعمال الحالية غير ملتزمة بعد (آخر التزام `23742a34`).
3. اختبار رفع براندنغ من `/master/settings` (يتطلب service-role + دلو `platform-assets` جاهزاً).
