# تقرير تفصيلي - الخطوة 5: الذراع التجاري ونظام الإعلانات المركزي
## منصة "ذكاء سهل" - السحابية متعددة المستأجرين

---

## 1. نظرة عامة

تم بناء نظام إعلانات مركزي متكامل (Centralized Advertising System) يوفّر ذراعاً تجارياً للمنصة، يتيح للعملاء طلب الإعلانات، وللمسؤولين (Master) إدارة الإعلانات والموافقة على الطلبات والتحكم بعرض الإعلانات في نقاط استراتيجية محددة. يعمل النظام بآلية طابور عرض ديناميكي مع خاصية التدوير التلقائي والإعلانات المثبتة (Pinned/Exclusive).

---

## 2. تعريفات الأنواع (Type Definitions)

**الملف:** `lib/ad-types.ts`

يشمل النظام مجموعة متكاملة من أنواع TypeScript لتوحيد البينات:

| النوع | الوصف |
|-------|-------|
| `AdPlacement` | موقع الإعلان في الصفحة: `'top' \| 'middle' \| 'bottom'` |
| `AdDisplaySpace` | مساحة العرض: `'Login' \| 'Full_Screen' \| 'Banner' \| 'Native'` |
| `AdPackage` | نوع الباقة الإعلانية: `'standard' \| 'exclusive' \| 'video'` |
| `AdLang` | اللغة: `'ar' \| 'en'` |
| `RequestStatus` | حالة الطلب: `'pending' \| 'approved' \| 'rejected'` |
| `AdStatus` | حالة الإعلان: `'active' \| 'inactive'` |
| `CtaType` | نوع زر الإجراء: `'visit' \| 'call' \| 'whatsapp' \| 'subscribe'` |
| `ClientInfo` | بيانات العميل: اسم النشاط التجاري، واتساب، قطاع الاستهداف |
| `CampaignInfo` | بيانات الحملة: تاريخ البدء/الانتهاء، نوع الباقة |
| `AdAttachments` | المرفقات: رابط الكارت الإعلاني، رابط إثبات الدفع |
| `DesignRequest` | طلب التصميم: النص التسويقي، رابط الشعار، الألوان المفضلة، نوع CTA |
| `AdRequest` | طلب إعلان كامل: يجمّع ClientInfo + CampaignInfo + AdAttachments + DesignRequest + الحالة والتواريخ |
| `AdConfig` | تهيئة الإعلان: العنوان، الوصف، الرابط الهدف، الموضع، مساحة العرض، اللغة، الحصرية، التثبيت، نوع CTA |
| `Ad` | كيان الإعلان الكامل: يجمّع AdConfig مع media_url, request_id, الحالة, النقرات, المشاهدات, الميزانية, المنصة, تاريخ الإنشاء |
| `KillSwitchState` | حالة الإيقاف الطارئ: `active: boolean`, `toggled_at: string` |

---

## 3. بيانات المحاكاة (Mock Data)

**الملف:** `lib/ads-mock-data.ts`

- **`adRequestsMock`**: 3 طلبات إعلان تجريبية (2 pending، 1 approved) لعملاء وهميين: "مخبز الأصيل"، "عيادة النور التخصصية"، "مكتبة المعرفة"
- **`adsMock`**: 3 إعلانات تجريبية بوضعيات مختلفة (top/middle/bottom) وحالات نشطة/متوقفة، مع إحصائيات نقرات ومشاهدات وميزانيات وهمية
- **`killSwitchMock`**: حالة Kill Switch افتراضية غير مفعلة

---

## 4. نظام API (Backend Routes)

### 4.1 مسار الإعلانات - `/api/master/ads/route.ts`
| الطريقة | الوظيفة | التحقق (Validation) |
|---------|---------|---------------------|
| `GET` | جلب جميع الإعلانات | - |
| `POST` | إنشاء إعلان جديد | Zod: title, description, targetUrl مطلوبة؛ + placement, display_space, lang, is_exclusive, is_fixed, cta_type, media_url, status |
| `PATCH` | تحديث إعلان موجود (معرفاً بـ id) | Zod: id مطلوب + تحديث جزئي لـ ad_config و status |

### 4.2 مسار طلبات العملاء - `/api/master/ads/requests/route.ts`
| الطريقة | الوظيفة | التحقق (Validation) |
|---------|---------|---------------------|
| `GET` | جلب جميع طلبات الإعلان | - |
| `POST` | إنشاء طلب إعلان جديد | Zod: client_info (business_name, whatsapp, target_sector), campaign (start_date, end_date, package) مطلوبة؛ attachments و design_request اختيارية |
| `PATCH` | الموافقة أو رفض طلب (تحديث status) | Zod: id مطلوب + status: 'approved' \| 'rejected' |

### 4.3 مسار Kill Switch - `/api/master/ads/kill-switch/route.ts`
| الطريقة | الوظيفة |
|---------|---------|
| `GET` | جلب حالة Kill Switch الحالية |
| `POST` | تبديل (Toggle) حالة Kill Switch مع تسجيل وقت التبديل |

**ملاحظة:** جميع الـ API Routes تستخدم حالياً بيانات محاكاة في الذاكرة (In-Memory Mock Data) وليس قاعدة البيانات الفعلية.

---

## 5. قاعدة البيانات (Supabase)

### 5.1 جدول `core.ads_engine`
يُستخدم لتخزين الإعلانات في Supabase. يتم جلب البيانات عبر الـ Hook المخصص.
- الحقول المطلوبة: `id, title, description, targetUrl, placement (top/bottom), lang (ar/en)`
- يتم الاستعلام عن الجدول عبر: `supabase.client.schema('core').from('ads_engine').select('*')`
- رابط الاستعلام: `hooks/use-projects.ts` (سطر 121)

### 5.2 الجداول الأخرى ذات العلاقة
- `core.project_definitions`: لربط الإعلانات بالقطاعات
- `core.users` (في `scripts/migration_auth.sql`): يحتوي على `push_tokens JSONB` و `metadata JSONB` يمكن استخدامها لاستهداف الإعلانات مستقبلاً

### 5.3 ملاحظات
- لا يوجد ملف هجرة (Migration) SQL مخصص لجداول الإعلانات بعد - النظام حالياً يعتمد على mock data
- الطلبات الحالية (pending/approved/rejected) لا تزال في الذاكرة وليست بقاعدة البيانات

---

## 6. مكونات الواجهة الأمامية (Frontend Components)

### 6.1 مكون العرض العام للإعلانات - `components/ad-renderer.tsx`
المكون الأساسي المسؤول عن عرض الإعلانات للزوار (Ad_Renderer_Component):

**الميزات:**
- **خاصية التصفية**: يقوم بتصفية الإعلانات حسب الموضع (placement) واللغة (lang)
- **الإعلانات المثبتة (Pinned/Exclusive)**: إذا وجد إعلان بـ `is_fixed` أو `is_exclusive` = true، يتم عرضه بشكل منفرد ويتجاوز طابور العرض
- **التدوير التلقائي (Auto-Slideshow)**: تدوير تلقائي كل 5 ثوانٍ في حالة وجود إعلانات متعددة غير مثبتة
- **التنقل اليدوي**: أزرار السابق/التالي ونقاط التصفح (Slider Dots)
- **الحركات (Animations)**: انتقالات سلسة باستخدام `motion` و `AnimatePresence`
- **التصميم**: بطاقة زجاجية (Glassmorphic) مع حدود ملونة ومساحة إعلانية مدمجة
- **زر CTA**: زر لفتح رابط الهدف في نافذة جديدة
- **ثابت AD_FALLBACK**: يحتوي على 6 إعلانات تجريبية (3 عربية + 3 إنجليزية) تُعرض في حال عدم توفر بيانات

**الاستخدام:**
```tsx
<Ad_Renderer_Component placement="top" lang={lang} ads={ads} />
<Ad_Renderer_Component placement="middle" lang={lang} ads={ads} />
<Ad_Renderer_Component placement="bottom" lang={lang} ads={ads} />
```

### 6.2 نافذة طلب إعلان (للعميل) - `components/ads/client-ad-request-modal.tsx`
نافذة منبثقة تسمح للعملاء بتقديم طلبات إعلان جديدة:

**أقسام النموذج:**
1. **بيانات الهوية**: اسم النشاط التجاري، رقم الواتساب، قطاع الاستهداف (قائمة منسدلة: التعليم/الصحة/العقارات/التجارة/الزراعة/السياحة/النقل/الضيافة)
2. **بيانات الحملة**: تاريخ البدء، تاريخ الانتهاء، اختيار الباقة (قياسي/حصري وثابت/فيديو قصير) مع وصف لكل باقة
3. **المرفقات**: رفع الكارت الإعلاني (WebP/PNG، حد أقصى 150KB)، رفع سند السداد المحلي مع معاينة الصور وإمكانية الحذف
4. **طلب التصميم (اختياري)**: النص التسويقي، الألوان المفضلة، نوع زر CTA (زيارة الموقع/اتصال هاتفي/واتساب/اشتراك) - قسم قابل للطي

**تدفق الإرسال:**
- تحقق من الحقول المطلوبة
- عرض حالة التحميل (Loading)
- عرض رسالة نجاح "تم استلام طلبك بنجاح"
- إعادة تعيين النموذج بعد الإغلاق

### 6.3 نافذة إدارة الإعلانات (للمسؤول) - `components/ads/master-ad-modal.tsx`
نافذة منبثقة تسمح للمسؤول بإنشاء/تحرير/الموافقة على الإعلانات:

**أنماط التشغيل:**
1. **إنشاء إعلان جديد**: نموذج فارغ
2. **تحرير إعلان موجود**: تعبئة البيانات من كائن Ad موجود
3. **الموافقة على طلب عميل**: تعبئة مسبقة من كائن AdRequest (اسم العميل، واتساب، الباقة، النص التسويقي، الكارت)

**حقول النموذج:**
- محتوى الإعلان: العنوان، الوصف، رابط الهدف، رابط الوسائط
- مفاتيح JSONB - إعدادات العرض: مساحة العرض (Login/Full Screen/Banner/Native)، موضع الصفحة (أعلى/وسط/أسفل)، اللغة
- تحديد نوع CTA
- التحكم في الظهور: حصرية الظهور (is_exclusive) مع تحذير عند التفعيل، تثبيت بصري (is_fixed) باستخدام ToggleSwitch
- حالة الإعلان: نشط/متوقف

### 6.4 لوحة تحكم الإعلانات - `app/master/ads/page.tsx`
الصفحة الرئيسية لإدارة الإعلانات في لوحة الماستر (`/master/ads`):

**الميزات:**
- **جلب البيانات**: استخدام React Query لجلب الإعلانات، الطلبات، وحالة Kill Switch
- **أزرار الإجراءات**:
  - "إضافة إعلان" ← يفتح MasterAdModal
  - "طلب إعلان (عميل)" ← يفتح ClientAdRequestModal
  - "Kill Switch" ← زر طارئ لإيقاف جميع الإعلانات
- **بطاقات الإحصائيات**:
  - إجمالي الإعلانات
  - إجمالي النقرات
  - إجمالي المشاهدات
  - الطلبات المعلقة
- **جدول طلبات العملاء المعلقة**: يعرض اسم النشاط، واتساب، القطاع، الباقة، التاريخ مع أزرار موافقة/رفض
- **جدول جميع الإعلانات**: يعرض العنوان، مساحة العرض، النقرات، المشاهدات، الحصرية، التثبيت، الحالة مع زر تحرير
- **Confirm Dialog**: نافذة تأكيد قبل الموافقة أو الرفض عبر GlassModal

**الدوال المساعدة (API Calls):**
- `fetchRequests()`, `fetchAds()`, `fetchKillSwitch()`: جلب البيانات
- `updateRequest(id, status)`: موافقة/رفض طلب
- `createAd(data)`, `updateAd(data)`: إنشاء/تحديث إعلان
- `toggleKillSwitch()`: تبديل Kill Switch

---

## 7. نقاط حقن الإعلانات (Ad Injection Points)

### 7.1 الصفحة الرئيسية - `app/page.tsx`
3 نقاط إعلانية:
1. **Top** (سطر 436): بعد الإحصائيات وقبل القطاعات
2. **Middle** (سطر 524): بين القطاعات ومعلومات Meta
3. **Bottom** (سطر 565): بعد معلومات Meta وقبل التذييل
- بالإضافة إلى رابط "أضغط هنا لإضافة إعلانك الخاص" (سطر 442) الذي يفتح ClientAdRequestModal

### 7.2 صفحة القطاع - `app/[sector_slug]/page.tsx`
3 نقاط إعلانية مطابقة:
1. Top (سطر 262)
2. Middle (سطر 271)
3. Bottom (سطر 274)

---

## 8. جلب البيانات والتخزين المؤقت (Data Fetching & Caching)

### 8.1 Hook: `useAds()` - `hooks/use-projects.ts`
- الاستعلام: `supabase.client.schema('core').from('ads_engine').select('*')`
- مفتاح التخزين المؤقت: `'ads'`
- وقت التحديث (Stale Time): 10 دقائق
- البيانات الاحتياطية (Fallback): `AD_FALLBACK` (6 إعلانات تجريبية)
- مفتاح التخزين في IndexedDB: `'ads_engine'`

### 8.2 دعم عدم الاتصال (Offline Support) - `hooks/use-projects.ts`
- دالة `fetchAdsWithCache()`: تتحقق من `navigator.onLine` أولاً
- إذا كان غير متصل: تعيد البيانات المخزنة مؤقتاً أو `AD_FALLBACK`
- التخزين في IndexedDB عبر `lib/db.ts` (دوال `cacheData`/`getCachedData`)

---

## 9. مكونات UI المساعدة

| المكون | المسار | الاستخدام في النظام الإعلاني |
|--------|--------|------------------------------|
| `ToggleSwitch` | `components/ui/toggle-switch.tsx` | تشغيل/إيقاف is_exclusive و is_fixed في MasterAdModal |
| `GlassModal` | `components/ui/glass-modal.tsx` | نافذة تأكيد الموافقة/الرفض في صفحة الإعلانات |

---

## 10. Kill Switch - الإيقاف الطارئ للإعلانات

آلية أمان تسمح للمسؤول بإيقاف جميع الإعلانات فوراً:
- زر Kill Switch في أعلى صفحة إدارة الإعلانات
- يتحول لون الزر إلى أحمر نابض عند التفعيل
- يتم تسجيل وقت التبديل (toggled_at)
- يمكن التبديل بين الفعّال/غير فعّال بنقرة واحدة
- لا يوجد حتى الآن تأثير فعلي على الـ Frontend (المنطق بحاجة للربط مع Ad_Renderer)

---

## 11. قائمة الملفات الكاملة

| المسار | الوصف |
|--------|-------|
| `lib/ad-types.ts` | تعريفات الأنواع الأساسية (Types) |
| `lib/ads-mock-data.ts` | بيانات محاكاة للإعلانات والطلبات و Kill Switch |
| `hooks/use-projects.ts` | Hook جلب الإعلانات من Supabase مع التخزين المؤقت |
| `components/ad-renderer.tsx` | مكون عرض الإعلانات العام مع التدوير والتثبيت |
| `components/ads/client-ad-request-modal.tsx` | نافذة تقديم طلب إعلان للعملاء |
| `components/ads/master-ad-modal.tsx` | نافذة إدارة الإعلانات للمسؤول |
| `components/ui/toggle-switch.tsx` | مفتاح التبديل المستخدم في إعدادات الإعلانات |
| `components/ui/glass-modal.tsx` | نافذة تأكيد الإجراءات |
| `app/api/master/ads/route.ts` | API: GET/POST/PATCH الإعلانات |
| `app/api/master/ads/requests/route.ts` | API: GET/POST/PATCH طلبات الإعلانات |
| `app/api/master/ads/kill-switch/route.ts` | API: GET/POST Kill Switch |
| `app/master/ads/page.tsx` | صفحة إدارة الإعلانات في لوحة الماستر |
| `app/master/page.tsx` | لوحة الماستر مع إحصائية الإعلانات النشطة |
| `app/master/components/nav-items.tsx` | قائمة التنقل مع عنصر "الإعلانات" |
| `app/page.tsx` | الصفحة الرئيسية مع 3 نقاط إعلانية |
| `app/[sector_slug]/page.tsx` | صفحة القطاع مع 3 نقاط إعلانية |
| `lib/db.ts` | نظام IndexedDB للتخزين المؤقت |
| `scripts/migration_auth.sql` | هجرة SQL (بدون جداول إعلانات مخصصة بعد) |
| `progress.md` | تتبع التقدم - الخطوة 5 مكتملة |

---

## 12. ملاحظات وتحسينات مستقبلية

- [ ] **ترحيل Mock Data إلى قاعدة البيانات**: نقل الـ API Routes من الذاكرة إلى Supabase
- [ ] **إنشاء SQL Migration**: إضافة جدول `ads_requests` و `ads_campaigns` إلى `scripts/`
- [ ] **ربط Kill Switch مع Ad_Renderer**: تطبيق تأثير Kill Switch على واجهة العرض
- [ ] **إحصائيات متقدمة**: رسوم بيانية وأدوات تحليل للأداء الإعلاني
- [ ] **رفع الملفات فعلياً**: استخدام Supabase Storage لرفع الكروت وإثباتات الدفع بدلاً من الرابط النصي
- [ ] **اختبارات آلية**: كتابة اختبارات للـ API Routes والمكونات
- [ ] **نظام دفع**: دمج بوابة دفع لتحصيل قيمة الباقات الإعلانية