# تقرير خطة تنفيذ لوحة الماستر (Master Dashboard)

## تاريخ التقرير: 23 يوليو 2026

---

## 1. فحص المشروع الحالي (Discovery)

### التقنيات المستخدمة
| التقنية | الإصدار |
|---|---|
| Next.js | 15.4.9 |
| React | 19.2.1 |
| TypeScript | 5.9.3 |
| Tailwind CSS | 4.1.11 |
| React Query | 5.101.2 |
| Motion (Framer Motion) | 12.23.24 |
| Lucide React | 0.553.0 |
| Supabase JS | 2.110.5 |

### الملاحظات الرئيسية
- المشروع يستخدم **Next.js 15 App Router** مع دعم كامل للـ RTL (عربي)
- لا يوجد `tailwind.config` — يستخدم **Tailwind v4** بالإعدادات من CSS
- **React Query** موجود مسبقاً مع `staleTime: 10 * 60 * 1000`
- الـ Auth نظام مخصص (JWT + bcryptjs) — لا يستخدم Supabase Auth
- الأدوار: `user`, `master`, `super_admin`
- يوجد `/master/login` لكن لا يوجد `/master` dashboard بعد
- المكتبات المتاحة: `lucide-react`, `motion`, `clsx + tailwind-merge`, `class-variance-authority`

### الثيمات الموجودة
- Light, Dark, Pink — يتم التبديل بـ `data-theme` على `<html>`
- كلاسات Glassmorphism جاهزة: `.glass-card`, `.glass-nav`, `.glass-modal`, `.glass-input`
- الخط: **Cairo** (يدعم العربي والإنجليزي)

---

## 2. هيكل الملفات الجديد (19 ملف)

```
app/master/
├── layout.tsx                          # Wrapper مع version switcher
├── page.tsx                            # Dashboard مع 6 كروت إحصائيات + رسوم بيانية وهمية
├── query-provider.tsx                  # QueryClient للماستر staleTime: 5min
├── versions/
│   ├── v1/
│   │   ├── MasterLayout.tsx            # Classic Sidebar 260px ثابت
│   │   ├── Sidebar.tsx                 # كامل الارتفاع، ملتصق بالحافة
│   │   └── Topbar.tsx                  # عنوان + theme + user + logout
│   ├── v2/
│   │   ├── MasterLayout.tsx            # Collapsible Sidebar
│   │   ├── Sidebar.tsx                 # 260px ← 64px مع motion spring
│   │   └── Topbar.tsx                  # + زر toggle ☰
│   └── v3/
│       ├── MasterLayout.tsx            # Floating Glass Sidebar
│       ├── Sidebar.tsx                 # بطاقة عائمة rounded-2xl مع glow
│       └── Topbar.tsx                  # Glass شفاف
├── components/
│   ├── nav-items.tsx                   # مصفوفة عناصر التنقل (6 items)
│   ├── version-switcher.tsx            # 3 أزرار v1/v2/v3
│   ├── stats-card.tsx                  # بطاقة إحصائية قابلة لإعادة الاستخدام
│   └── sidebar-logo.tsx               # Logo/Name للنسخ الثلاث
├── projects/page.tsx                   # صفحة المشاريع (جدول وهمي)
├── ads/page.tsx                        # صفحة الإعلانات (بطاقات وهمية)
├── features/page.tsx                   # صفحة الميزات
├── users/page.tsx                      # صفحة المستخدمين (جدول وهمي)
└── settings/page.tsx                   # صفحة الإعدادات
```

---

## 3. النسخ الثلاث — جدول المقارنة

| الخاصية | v1 Classic | v2 Collapsible | v3 Floating |
|---|---|---|---|
| **عرض الـ Sidebar** | 260px ثابت | 260px / 64px (مصغر) | 260px |
| **الالتصاق بالحافة** | نعم (يمين RTL) | نعم | **لا** — مسافة 16px |
| **الزوايا** | `rounded-0` | `rounded-0` | `rounded-2xl` (دائرية) |
| **الخلفية** | `bg-[#0a0e1a]` صلب | `bg-[#0a0e1a]` صلب | `glass-card` مع `backdrop-blur-2xl` |
| **التوهج (Glow)** | لا | لا | `shadow-[0_0_40px_var(--glow-color)]` |
| **Motion** | تبديل الصفحات | Spring collapse + tooltip | Hover glow + translate-x |
| **الطابع** | Swiss صارم ونظيف | Swiss + ديناميكي | Swiss + فخامة مستقبلية |
| **User card** | في الـ Sidebar | في الـ Sidebar (مصغر) | في الـ Topbar |

### v1 — Classic Sidebar (تقليدي احترافي)
```
┌──────────┬──────────────────────────────────────┐
│  LOGO    │  لوحة القيادة          ☀ 👤 🚪     │
│  ●───    │  [عنوان الصفحة]                      │
│  لوحة    │──────────────────────────────────────│
│  مشاريع  │  ┌─────┐ ┌─────┐ ┌─────┐           │
│  إعلانات │  │12.8K│ │ 48  │ │ 124 │           │
│  ميزات   │  └─────┘ └─────┘ └─────┘           │
│  مستخدمين│  ┌─────┐ ┌─────┐ ┌─────┐           │
│  إعدادات │  │$284K│ │ 36  │ │94.2%│           │
│  ────────│  └─────┘ └─────┘ └─────┘           │
│  👤 أحمد │  [آخر النشاطات...]                   │
└──────────┴──────────────────────────────────────┘
```

- Sidebar كامل الارتفاع، لون داكن صلب
- Logo في الأعلى مع خط فاصل
- 6 عناصر تنقل مع أيقونة + نص + Active indicator (شريط عمودي + توهج)
- User card في الأسفل: صورة + اسم + دور
- Topbar بسيط: عنوان الصفحة (يسار) + ThemeSwitcher + اسم المستخدم + زر logout (يمين)
- المحتوى: Grid 3×2 للبطاقات الإحصائية

### v2 — Collapsible Mini Sidebar (ديناميكي عصري)
```
[موسع]                         [مصغر]
┌──────────┬────────────────┐   ┌─────┬─────────────────────┐
│ ☰ LOGO   │  لوحة القيادة  │   │ ☰   │  لوحة القيادة       │
│ ●───     │                │   │ ●   │                     │
│ ◼ لوحة   │  [المحتوى]     │   │ ◼   │  [المحتوى]          │
│ 📁 مشا   │                │   │ 📁  │                     │
│ 📢 إعلا   │                │   │ 📢  │                     │
│ ⚡ ميزا   │                │   │ ⚡  │                     │
│ 👥 مستخ   │                │   │ 👥  │                     │
│ ⚙ إعدا   │                │   │ ⚙  │                     │
│ ────────  │                │   │ ─── │                     │
│ 👤 أحمد   │                │   │ 👤  │                     │
└──────────┴────────────────┘   └─────┴─────────────────────┘
```

- زر ☰ في أعلى الـ Sidebar أو الـ Topbar
- `motion.div` مع `animate={{ width }}` و `transition={{ type: "spring", stiffness: 300, damping: 30 }}`
- عند التصغير: الأيقونات تبقى والنصوص تختفي مع fade
- **Tooltip** عند hover على الأيقونة في الوضع المصغر
- User card يتحول إلى أيقونة فقط
- كل المحتوى يتمدد تلقائياً مع تغير عرض الـ Sidebar

### v3 — Floating Glass Sidebar (مستقبلي فاخر)
```
┌──────┬───────────────────────────────────────────┐
│      │  ☰  لوحة القيادة     ☀ 👤 أحمد 🚪      │
│ ┌──┐ │───────────────────────────────────────────│
│ │لـ│ │  ┌─────┐ ┌─────┐ ┌─────┐                │
│ │و│ │  │12.8K│ │ 48  │ │ 124 │                │
│ │ح│ │  └─────┘ └─────┘ └─────┘                │
│ │ة│ │  [المحتوى...]                            │
│ │ │ │                                           │
│ │ │ │                                           │
│ └──┘ │                                           │
└──────┴───────────────────────────────────────────┘
```

- Sidebar **غير ملتصق** بالحافة: `top-4 right-4 bottom-4` (أو `right-4` في RTL)
- `rounded-2xl` + `backdrop-blur-2xl` + `bg-white/5` أو `bg-black/30`
- `shadow-[0_8px_32px_rgba(99,102,241,0.15)]` توهج indigo خفيف
- `border border-white/10`
- كل عنصر hover: `translate-x-[-4px]` (RTL) + `shadow-[0_0_20px_rgba(99,102,241,0.3)]`
- الـ Topbar: Glass شفاف مع `backdrop-blur-xl` يمتد للعرض الكامل
- خلفية اللوحة: تدرج داكن + نقاط Grid خفيفة (dot pattern) للعمق

---

## 4. عناصر التنقل (Nav Items)

| الرمز | الاسم (عربي) | الاسم (إنجليزي) | المسار | الأيقونة |
|---|---|---|---|---|
| 1 | لوحة القيادة | Dashboard | `/master` | `LayoutDashboard` |
| 2 | المشاريع | Projects | `/master/projects` | `FolderKanban` |
| 3 | الإعلانات | Ads | `/master/ads` | `Megaphone` |
| 4 | الميزات | Features | `/master/features` | `Zap` |
| 5 | المستخدمون | Users | `/master/users` | `Users` |
| 6 | الإعدادات | Settings | `/master/settings` | `Settings` |

### Active Indicator
- **v1, v2**: شريط عمودي (2px) على يمين العنصر النشط مع توهج indigo
- **v3**: شريط عمودي + توهج خلفية (`bg-indigo-500/10`) + إزاحة لليسار قليلاً

---

## 5. الـ Topbar (مشترك مع تخصيصات)

```
[← RTL]
[v1/v2]                           [v3]
┌──────────────────────────┐     ┌──────────────────────────┐
│ لوحة القيادة         ☀ 👤│     │ ☰ لوحة القيادة     ☀ 👤│
│                        🚪│     │ أحمد        🔔      🚪│
└──────────────────────────┘     └──────────────────────────┘
   عنوان الصفحة فقط               Glass شفاف + اسم + إشعارات
```

### العناصر
| العنصر | v1 | v2 | v3 |
|---|---|---|---|
| زر toggle (☰) | ❌ | ✅ (يمين RTL) | ✅ (يمين RTL) |
| عنوان الصفحة | ✅ | ✅ | ✅ |
| Breadcrumb | ❌ | ❌ | ✅ (تحت العنوان) |
| Search | ❌ | ❌ | ✅ (أيقونة) |
| ThemeSwitcher | ✅ | ✅ | ✅ |
| اسم المستخدم | ✅ | ✅ | ✅ (بجانب الصورة) |
| صورة المستخدم | ❌ | ❌ | ✅ (دائرية) |
| زر الإشعارات | ❌ | ❌ | ✅ (مع Badge) |
| قائمة منسدلة (Logout) | ✅ عند الضغط على الاسم | ✅ | ✅ |

### الـ ThemeSwitcher
- 3 أيقونات: ☀ (Light) / 🌙 (Dark) / 🩷 (Pink)
- يستخدم `toggleTheme()` الموجود في `useApp()` من `providers.tsx`
- مؤشر دائري على الثيم النشط

---

## 6. صفحات الـ Routing

### `/master` — Dashboard (لوحة القيادة)
```tsx
// Mock Data:
const stats = [
  { title: 'المستخدمون النشطون', value: '12,847', change: '+12%', up: true },
  { title: 'المشاريع الجارية',    value: '48',    change: '+3',   up: true },
  { title: 'الإعلانات النشطة',    value: '124',   change: '-2%',  up: false },
  { title: 'الإيرادات الشهرية',   value: '$284K', change: '+8%',  up: true },
  { title: 'الميزات المطلوبة',    value: '36',    change: 'جديد', up: true },
  { title: 'معدل الرضا',          value: '94.2%', change: '+1.2%', up: true },
]
```

6 بطاقات إحصائية بتنسيق Swiss Grid (3 أعمدة في desktop، 2 في tablet، 1 في mobile).

- كل بطاقة: أيقونة + قيمة + عنوان + نسبة التغير مع سهم (⬆ / ⬇)
- لون التغير: أخضر للإيجابي، أحمر للسلبي
- Glassmorphism خفيف مع hover effect

### صفحات فرعية
| المسار | المحتوى |
|---|---|
| `/master/projects` | جدول مشاريع (5 صفوف وهمية: الاسم، الحالة، التقدم، تاريخ البدء) |
| `/master/ads` | 3 بطاقات إعلانات (العنوان، المنصة، الميزانية، النقرات) |
| `/master/features` | 4 بطاقات ميزات (الاسم، الوصف، الأولوية، الحالة) |
| `/master/users` | جدول مستخدمين (5 صفوف: الاسم، الهاتف، الدور، الحالة) |
| `/master/settings` | 3 أقسام إعدادات (عامة، واجهة، أمان) |

كل صفحة لها:
- **Skeleton loading** عند أول تحميل (باستخدام `motion` مع `animate-pulse`)
- **عنوان الصفحة** في الـ Topbar يتغير ديناميكياً
- تصميم Swiss Style: `grid-cols-12` مع `gap-6`

---

## 7. React Query — Master QueryClient

سيتم إنشاء `app/master/query-provider.tsx`:

```ts
const masterQueryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,  // 5 دقائق كما طلبت
      retry: 1,
      refetchOnWindowFocus: false,
    },
  },
})
```

ويتم تغليف كل مسارات `/master/*` به:
```tsx
// app/master/layout.tsx
<QueryClientProvider client={masterQueryClient}>
  <MasterLayoutWithSwitcher />
</QueryClientProvider>
```

**ملاحظة:** سيتم استخدام QueryClient منفصل عن الـ Root QueryClient (الذي لديه staleTime 10min) لتجنب التعارض.

---

## 8. الـ Version Switcher (تبديل النسخ)

Component: `app/master/components/version-switcher.tsx`

```tsx
// 3 أزرار في أعلى اللوحة (فقط لل development/testing)
// v1 | v2 | v3
// الزر النشط: لون مختلف (indigo) مع border ساطع
// الزر غير النشط: شفاف مع hover effect
// يخفي نفسه في production (NEXT_PUBLIC_SHOW_VERSIONS)
```
- 3 أزرار صغيرة: `v1` | `v2` | `v3`
- الزر النشط بلون indigo مع توهج خفيف
- مخفية تلقائياً إذا لم يكن `NEXT_PUBLIC_SHOW_VERSIONS=true`
- حالة النسخة الحالية محفوظة في `localStorage`

---

## 9. الـ CSS والإضافات البصرية

### Swiss Style
- **Grid صارم**: استخدم `grid-cols-12` مع `gap-4` في المحتوى، `gap-6` بين الأقسام
- **محاذاة دقيقة**: `items-start`, `justify-between`, مسافات متسقة (multiples of 4)
- **Hierarchy typographic**: تدرج واضح في أحجام الخطوط (h1: 2xl, h2: xl, h3: lg, body: base)

### Dark Futuristic
- الخلفية الرئيسية: `#090d16` (أو أغمق `#070b14`)
- توهجات خلفية خفيفة: radial gradient في زاوية اللوحة
- لمسات Indigo: `#6366f1` كلون مميز للماستر (يختلف عن primary blue العادي)
- الخطوط: أرقام واضحة مع `font-mono` للإحصائيات

### Glassmorphism
- استخدام `backdrop-blur` الموجود مسبقاً
- إضافة كلاس `.glass-sidebar` خاص بالماستر
- `border-white/[0.05]` إلى `border-white/[0.15]`
- ظل زجاجي: `shadow-[0_8px_32px_rgba(0,0,0,0.4)]`

---

## 10. سير العمل بعد الموافقة

```
1. الموافقة على الخطة
       │
2. إنشاء 19 ملف (بالترتيب):
   ├── components/ (nav-items, version-switcher, stats-card, sidebar-logo)
   ├── query-provider.tsx
   ├── v1/ (3 ملفات)
   ├── v2/ (3 ملفات)
   ├── v3/ (3 ملفات)
   ├── layout.tsx + page.tsx
   └── 5 صفحات فرعية
       │
3. npm run dev (تشغيل الخادم)
       │
4. فتح Chrome DevTools:
   ├── فتح localhost:3000/master
   ├── تجربة النسخ الثلاث (v1 ↔ v2 ↔ v3)
   ├── تبديل الثيم (dark/light/pink)
   ├── التنقل بين الصفحات
   ├── فحص Console (لا أخطاء)
   └── فحص Layout بصرياً
       │
5. إصلاح أي أخطاء
       │
6. تسليم النتيجة النهائية
```

---

## 11. الأسئلة التي تمت الإجابة عليها

| السؤال | الإجابة |
|---|---|
| طريقة اختيار النسخة؟ | Switcher داخل اللوحة (v1/v2/v3) |
| محتوى Dashboard؟ | Mock Data كاملة مع 6 بطاقات إحصائية |
| الصفحات الفرعية؟ | Routes حقيقية (5 صفحات) |

---

## 12. ملاحظات إضافية

- المشروع **RTL** بالكامل (عربي) — كل التصميم سيأخذ هذا بعين الاعتبار
- **لا مكتبات إضافية** مطلوبة — كل شيء موجود (motion, lucide-react, tailwind, cn)
- Auth متكامل مع `useAuth()` — جاهز لربط المستخدم الحقيقي
- الـ Sidebar سيختفي تلقائياً على الشاشات الصغيرة (< 768px) مع `use-mobile` hook الموجود
- كل النسخ تستخدم نفس ملف `nav-items.tsx` لتجنب تكرار الكود

---

---

## 13. نتائج التنفيذ والفحص — ✅ تم بنجاح

### الملفات المنشأة (21 ملف)

| المسار | الوصف |
|---|---|
| `app/master/layout.tsx` | Root layout مع VersionSwitcher + Auth guard |
| `app/master/page.tsx` | Dashboard مع 6 بطاقات إحصائية + نشاطات + مشاريع |
| `app/master/components/nav-items.tsx` | 6 عناصر تنقل + `getPageTitle()` + `useNavItems()` |
| `app/master/components/version-switcher.tsx` | 3 أزرار v1/v2/v3 (يظهر فقط في development) |
| `app/master/components/stats-card.tsx` | بطاقة إحصائية مع motion أنميشن |
| `app/master/components/sidebar-logo.tsx` | Logo + اسم المنصة |
| `app/master/versions/v1/Sidebar.tsx` | Classic Sidebar — ثابت 260px، indicator مع `layoutId` |
| `app/master/versions/v1/Topbar.tsx` | عنوان + ThemeSwitcher + اسم المستخدم + logout |
| `app/master/versions/v1/MasterLayout.tsx` | Flex container + `mr-[260px]` |
| `app/master/versions/v2/Sidebar.tsx` | Collapsible — 260px ← 64px، motion spring |
| `app/master/versions/v2/Topbar.tsx` | نفس v1 |
| `app/master/versions/v2/MasterLayout.tsx` | يدير `collapsed` state ويغير `marginRight` |
| `app/master/versions/v3/Sidebar.tsx` | Floating — `right-4 top-4 bottom-4`، `rounded-2xl`، توهج |
| `app/master/versions/v3/Topbar.tsx` | Glass شفاف + زر إشعارات + قائمة منسدلة للمستخدم |
| `app/master/versions/v3/MasterLayout.tsx` | خلفية مع glow + dot grid pattern |
| `app/master/projects/page.tsx` | جدول مشاريع مع شريط تقدم متحرك |
| `app/master/ads/page.tsx` | إحصائيات + جدول إعلانات |
| `app/master/features/page.tsx` | 6 بطاقات ميزات مع ألوان حسب الحالة |
| `app/master/users/page.tsx` | إحصائيات + جدول مستخدمين |
| `app/master/settings/page.tsx` | 3 أقسام إعدادات مع ThemeSwitcher |
| `app/master/login/page.tsx` | موجود مسبقاً — صفحة دخول الماستر |

### نتائج البناء (Build)

| الفحص | النتيجة |
|---|---|
| `npm run build` | ✅ Compiled successfully — 0 errors |
| TypeScript | ✅ No type errors |
| Static pages generated | ✅ 19/19 pages |
| Linting | ✅ Skipped (حسب الإعدادات) |

### نتائج الفحص بالمتصفح (Chrome DevTools MCP)

| الفحص | النتيجة |
|---|---|
| التوجيه (RTL) | ✅ `dir="rtl"` |
| اللغة | ✅ `lang="ar"` |
| الثيم الافتراضي | ✅ `data-theme="dark"` |
| الخط | ✅ Cairo variable font |
| لون الخلفية | ✅ `#090d16` (داكن) |
| أخطاء Console | ✅ 0 أخطاء |
| HTTP 200 لجميع المسارات | ✅ 6/6 مسارات |
| صفحة تسجيل الدخول | ✅ تعمل وتظهر بشكل صحيح |
| إعادة التوجيه للمستخدم غير الموثّق | ✅ تعمل (`/master` → `/master/login`) |
| أحجام الصفحات مختلفة | ✅ تأكيد تنوع المحتوى لكل صفحة |

### التعديلات على الخطة الأصلية

| البند | الخطة | التنفيذ |
|---|---|---|
| React Query Provider | منفصل للماستر staleTime 5min | ❌ أُلغيت — استخدام الـ Root Provider مع staleTime على مستوى الـ hooks |
| Version Switcher | 3 أزرار | ✅ في development فقط (يخفي نفسه في production) |
| Mock Data | 6 بطاقات | ✅ 6 بطاقات + نشاطات + مشاريع |
| صفحات فرعية | Routes حقيقية | ✅ 5 صفحات مع محتوى وهمي |

**تم التنفيذ والفحص — الاختيار الآن لك.**
