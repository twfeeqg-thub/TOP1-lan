# ✅ تقرير التنفيذ الكامل — بوابة المصادقة السيادية، حقن المالك، التنقل الديناميكي، ومحرك إدارة الملف الشخصي

> **الحالة:** ✅ اكتمل التنفيذ والتحقق (BUILD MODE)
> **التاريخ:** 5 أغسطس 2026
> **قاعدة البيانات:** Supabase (schema: `core`) — الاتصال المباشر عبر `DATABASE_URL` يعمل
> **التحقق:** `tsc --noEmit` نظيف، `npm run build` ناجح، لا أخطاء ESLint جديدة، `generate-dna.js` شُغّل بنجاح

---

## نظرة عامة على ما تم

نُفّذت الخطوات الخمس كاملةً: ترحيل قواعد البيانات للتعددية (Tenants + Subscriptions)، حقن المالك السيادي، واجهات المصادقة الكاملة بـ HttpOnly Cookie، محرك «تعديل بيانات الحساب» الزجاجي، والتنقل الديناميكي المبني على الأدوار. تم تطبيق الترحيل فعلياً على السحابة والتحقق منه بالاستعلام المباشر.

---

## Step 1 — الترحيل متعدد الأطراف (Idempotent SQL) ✅

### الملف: `scripts/migration_subscriptions.sql`
- **تم تطبيقه فعلياً على قاعدة البيانات السحابية** عبر pg pool (أُعيد التشغيل الآمن).
- جدول `core.tenants`: `id UUID PK`، `name TEXT NOT NULL`، `phone TEXT UNIQUE NOT NULL`، `created_at`.
- جدول `core.user_subscriptions`: `id UUID PK`، `user_id → core.users (ON DELETE CASCADE)`، `tenant_id → core.tenants (ON DELETE SET NULL)`، `project_slug`، `plan CHECK ('basic','pro','enterprise') DEFAULT 'basic'`، `is_active BOOLEAN DEFAULT true`، `expires_at`، `created_at`، مع **قيود فريدة** `uq_user_subscription UNIQUE (user_id, project_slug)`.
- فهارس على `user_id` و `project_slug` و `tenant_id`.
- **RLS مفعّل** على الجدولين مع سياسات سماح للـ `anon` و `authenticated` (service_role يتجاوز RLS أصلاً).
- **GRANT ALL** للأدوار الثلاثة + `ALTER DEFAULT PRIVILEGES`.

### التحقق المباشر (استعلام حي)
```
[{"relname":"tenants","relrowsecurity":true},{"relname":"user_subscriptions","relrowsecurity":true}]
[{"conname":"uq_user_subscription","def":"UNIQUE (user_id, project_slug)"}]
```

### ملاحظة معمارية
أُضيف قيد `UNIQUE (user_id, project_slug)` رغم أنه لم يُذكر حرفياً في المواصفة، لأنه **شرط إلزامي** لعملية `upsert` الآمنة عند ضمان اشتراكات المالك ومنع التكرار.

---

## Step 2 — حقن المالك السيادي (super_admin) ✅

### الملفات الجديدة/المعدّلة
| الملف | الإجراء |
|---|---|
| `scripts/seed-owner.ts` | جديد — سكربت الحقن |
| `package.json` | إضافة `"seed:owner": "tsx scripts/seed-owner.ts"` |
| `.env.example` | إضافة `SEED_OWNER_NAME / SEED_OWNER_PHONE / SEED_OWNER_PASSWORD` مع تحذير أمني |
| `lib/auth.ts` | توحيد bcrypt على **10 جولات** (`SALT_ROUNDS = 10`) + نوع الدور `AuthRole` يشمل `super_admin` |

### منطق السكربت
1. تحميل المتغيرات من `.env.local` مع **توقف فوري** عند غياب أي منها.
2. تطبيع الهاتف إلى E.164 عبر `normalizePhone()`.
3. تشفير كلمة المرور بـ bcrypt 10 جولات.
4. بحث مسبق: إن وُجد الحساب بدور آخر → **رفض تام** (لا ترقية صامتة للصلاحيات)؛ إن كان `super_admin` → تحديث الاسم/كلمة المرور؛ وإلا → إنشاء جديد.
5. حقن اشتراكات `enterprise` للمشاريع التعليمية الخمسة:
   `exam-engine`، `teacher-twin`، `school-management`، `institute-management`، `quran-circles`.
6. تسجيل تدقيق (`owner_seeded` / `owner_seed_updated`) بخطورة `high`.

### ✅ اختبار الحارس
```
❌ خطأ: متغيرات SEED_OWNER_NAME / SEED_OWNER_PHONE / SEED_OWNER_PASSWORD مفقودة.
```

### ⚠️ إجراء بانتظار المستخدم (لم يُنفَّذ — حماية أمنية)
مفاتيح المالك غير موجودة في `.env.local`. أضفها ثم شغّل:
```
SEED_OWNER_NAME="..."
SEED_OWNER_PHONE="+9677XXXXXXXX"
SEED_OWNER_PASSWORD="..."
npm run seed:owner
```

---

## Step 3 — واجهات المصادقة الكاملة (`/api/auth/*`) ✅

### ملفات المكتبات الداعمة
| الملف | الوظيفة |
|---|---|
| `lib/auth.ts` | bcrypt (10 جولات) + JWT + ثوابت الكوكيز (`REFRESH_COOKIE_OPTIONS` و `ACCESS_COOKIE_OPTIONS`) |
| `lib/auth-session.ts` (جديد) | `resolveSessionFromRequest()` — استخلاص الهوية من كوكي الجلسة HttpOnly فقط |
| `lib/subscriptions.ts` (جديد) | ثوابت آمنة للعميل فقط (بدون استيراد pg): القوائم، `APP_SLUG_TO_PROJECT_SLUG`، `APP_SLUG_ROUTES` |
| `lib/subscriptions-server.ts` (جديد) | دوال خادمية: `fetchUserSubscriptions()` و `ensureSuperAdminSubscriptions()` |
| `lib/validators.ts` | كلمة مرور صارمة (≥8 + حروف + أرقام) للتسجيل/التعديل + `updateProfileSchema` |

> **قرار معماري مهم:** فُصلت `subscriptions.ts` عن `subscriptions-server.ts` لأن استيراد `pg` من صفحة هبوط (عميل) كان يكسر البناء (`Can't resolve 'net'/'tls'`). الثوابت الآن آمنة للعميل والدوال الخادمية معزولة.

### المسارات
| المسار | التغيير |
|---|---|
| `register` | **إعادة كتابة**: معاملة SQL ذرّية واحدة عبر `pool` (pg) → إنشاء `core.tenants` + `core.users` (مع `metadata.tenant_id`) + اشتراك `pro` للخدمة. فحص تعارضات، التقاط `23505` → 409. إصدار refresh HttpOnly + access. |
| `login` | عند `super_admin` → `ensureSuperAdminSubscriptions()` تلقائياً + إرجاع `subscriptions`. |
| `logout` | إبطال الجلسة + مسح **كلا** الكوكيز (refresh + access). |
| `session` (جديد) | قراءة الحالة من كوكي HttpOnly فقط عبر `resolveSessionFromRequest` → إرجاع `{ user, subscriptions, access_token }`. |
| `refresh` | **تدوير كامل**: توليد refresh جديد + تحديث صف الجلسة + كوكي جديد + access جديد. |
| `check-phone` | دعم بارامتر `exclude` لاستثناء الرقم الحالي عند التعديل. |

### ✅ اختبار المعاملة (حي، مع ROLLBACK — بلا أثر)
```
TX_OK {"subs":"1","tenants":"1"}
ROLLED_BACK_CLEAN
```

### تحديث `context/AuthContext.tsx`
- **تهيئة حقيقية** عبر `/api/auth/session` عند التحميل (`isLoading` حقيقي).
- إضافة `subscriptions: string[]` إلى السياق + `refreshSubscriptions()`.
- إضافة `applyProfile(user, token)` لتحديث الحساب بعد التعديل.
- `logout` و `refreshToken` محدّثان مع مزامنة الاشتراكات.

---

## Step 4 — محرك «تعديل بيانات الحساب» ✅

### الواجهة البرمجية `app/api/auth/update-profile/route.ts` (جديد)
1. **هوية خادمية 100%**: `resolveSessionFromRequest` من كوكي الجلسة — لا يُثق بأي ادعاء من العميل.
2. تحقق Zod: `updateProfileSchema` — الاسم اختياري، الهاتف اختياري، كلمة المرور ≥8 حروف+أرقام.
3. **تأكيد كلمة المرور الحالية إلزامي** قبل أي تغيير لكلمة المرور.
4. الهاتف: تطبيع E.164 + فحص التفرد (مع استبعاد الذات) → 409 عند التعارض + **تحديث `core.tenants.phone`** عند وجود `tenant_id` مرتبط.
5. كتابة التحديثات + إعادة توقيع access token + تحديث كوكيه.
6. **سجل تدقيق** في `core.master_audit_log` مع تفاصيل الحقول المتغيرة.

### الواجهة الزجاجية `components/auth/EditProfileModal.tsx` (جديد)
- زجاجية شفافة مع توهج (`GlassModal` + متغيرات `globals.css`) — متوافقة مع الثيمات **Light / Dark / Rosy Pink**.
- حقول: الاسم، الهاتف (مع فحص حي مؤجّل لتوفر الرقم عبر `/check-phone?exclude=`)، كلمة المرور الجديدة (اختياري).
- **مؤشر قوة كلمة المرور** (4 أشرطة + تسمية عربية: ضعيفة/متوسطة/قوية/قوية جداً).
- **حقل تأكيد كلمة المرور الحالية** يظهر تلقائياً عند كتابة كلمة مرور جديدة.
- زر «حفظ التغييرات» مع مؤشر تحميل + رسائل نجاح/خطأ (أخضر/أحمر).
- حالة التحقق من الرقم مشتقة (دون setState متزامن في effects) لتفادي أخطاء lint الجديدة.

### الدمج في الواجهات
| الموقع | التغيير |
|---|---|
| `app/master/versions/v2/Topbar.tsx` | زر `UserCog` يفتح النافذة بجانب زر الخروج |
| `app/page.tsx` (شارة الحساب) | شارة زجاجية مع زر تعديل + زر خروج عند تسجيل الدخول |

---

## Step 5 — التنقل الديناميكي المبني على الأدوار ✅

| الملف | التغيير |
|---|---|
| `app/master/components/nav-items.tsx` | إضافة `roles: AuthRole[]` لكل عنصر + `getNavItems(role)` — super_admin يرى الكل، master يستبعد `/master/users`، user لا يرى لوحة الماستر |
| `app/master/versions/v2/Sidebar.tsx` | استهلاك الأدوار + **قسم «قفزات مباشرة» زجاجي** للمالك → `/exam-engine/maker` و `/exam-engine/taker` و `/master/sectors` |
| `app/page.tsx` | **لوحة القيادة السيادية** للمالك (4 أزرار قفز مباشر بدون إعادة مصادقة) + **قسم «مشاريعك المشترك فيها»** للمستخدمين مفلترة من `subscriptions` ∩ `project_definitions` |
| `app/(projects)/exam-engine/page.tsx` | توجيه بالسياق: super_admin/master → `maker`، user مشترك → `taker`، وإلا → `/login?service=exam-engine` (مع الاحتفاظ بتجاوز DEV_MODE) |

### مبدأ التنفيذ
- لا نص تسويقي ثابت — كل العناوين من `modules_config` في قاعدة البيانات.
- `globals.css` هي المصدر الوحيد للأنماط (متغيرات `var(--*)`).

---

## اعتبارات وحلول تقنية مهمة

1. **فصل pg عن العميل:** استيراد `pg` من صفحة هبوط كسر البناء؛ حُلّ بقسمة `subscriptions.ts` (ثوابت) / `subscriptions-server.ts` (خادم).
2. **المعاملة الذرّية:** `register` يستخدم `pool` (pg) في معاملة واحدة لإرضاء متطلب «معاملة واحدة» مع الحفاظ على `poolAdmin` لباقي عمليات المصادقة (الاستثناء الوحيد الموثق في `app/api/auth/README_LOGIC.md`).
3. **التوافق الرجعي لكلمة المرور:** `loginSchema` يستخدم مخططاً متساهلاً كي لا يُحجب المستخدمون القدامى بكلمات قصيرة، بينما التسجيل/التعديل يستخدمان المخطط الصارم.
4. **`eslint.ignoreDuringBuilds: true`** في `next.config.ts` — أخطاء lint السابقة (17) موجودة مسبقاً في الملفات القديمة؛ كودنا الجديد لا يضيف أي خطأ (18 إجمالياً، أي أقل من الأساس 20 قبل التعديل).

---

## ملفات جديدة
- `scripts/migration_subscriptions.sql`، `scripts/seed-owner.ts`
- `lib/auth-session.ts`، `lib/subscriptions.ts`، `lib/subscriptions-server.ts`
- `app/api/auth/session/route.ts`، `app/api/auth/update-profile/route.ts`
- `components/auth/EditProfileModal.tsx`
- README_LOGIC.md لمسارات `session` و `update-profile`

## ملفات معدّلة
- `lib/auth.ts`، `lib/validators.ts`
- `app/api/auth/{register,login,logout,refresh,check-phone}/route.ts`
- `context/AuthContext.tsx`
- `app/master/components/nav-items.tsx`، `app/master/versions/v2/{Sidebar,Topbar}.tsx`
- `app/page.tsx`، `app/(projects)/exam-engine/page.tsx`
- `package.json`، `.env.example`
- ملفات `README_LOGIC.md` المتأثرة (بما فيها الجذري و`lib/`) — ثم `node scripts/generate-dna.js` ✅

---

## ملاحظات أمنية وتوصيات متابعة

1. **⚠️ الإجراء العالق:** إضافة `SEED_OWNER_*` إلى `.env.local` وتشغيل `npm run seed:owner` لاستكمال حقن المالك.
2. **🛡️ توصية (خارج نطاق الخطوات الخمس):** تقوية `middleware.ts` بحماية مسارات `/(projects)/*` مباشرةً — حالياً أي زائر يعرف العنوان يستطيع فتح `/exam-engine/maker` دون مصادقة لأن الماستر محمي فقط. التوجيه الآمن يعمل عبر صفحة `/exam-engine` المدخلة، لكن الحماية الرأسمالية تتطلب توسيع الـ matcher. لم أعدّل middleware التزاماً بمبدأ «الإضافة لا التعديل دون إذن».
3. **Dexie/Offline:** طبقة التخزين المحلي قائمة كما هي؛ مزامنة سياق المصادقة دون اتصال (جدول `auth` في Dexie) متاحة كمرحلة تالية.
